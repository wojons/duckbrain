{
  "task_id": "DB-SUPA-6",
  "task_title": "Persistent declared DDL and safe schema evolution",
  "task_criteria": [
    "**AC-1 (persistent declaration and types):** GIVEN a valid v1 `schema.json` containing object and positional tables across every declared type, WHEN a fresh process loads the namespace, THEN it reconstructs the same declared validators, storage paths, keys, headers, canonical type representations, and table versions in `TableSchemaRegistry` without inferring a data line or coercing a value. **Checks:** `schema-registry.test.ts` / `\"loads v1 object and positional declarations after restart\"`; `schema-json.test.ts` / `\"rejects unknown fields and invalid declarations\"`; `schema-types.test.ts` / `\"declared types use canonical JSONL and API representations\"`.",
    "**AC-2 (schema/manifest separation):** GIVEN a namespace with both metadata files, WHEN a table is created, a row is written, and a partition is added, THEN `schema.json` contains only logical declarations, `manifest.json` remains only partitions plus `lastUpdated`, and historical manifest reading in `src/git/asof.ts` remains compatible. **Checks:** `ddl-manifest-contract.test.ts` / `\"schema and manifest retain separate ownership\"`; `asof-ddl-compat.test.ts` / `\"historical manifest remains readable\"`.",
    "**AC-3 (atomic authorized create):** GIVEN an admin and a non-admin principal, WHEN both attempt the same create-table request, THEN only the admin can create it; a retry with the same idempotency key and canonical request returns the original declaration, while a different declaration for that name is rejected without modifying either metadata file. **Checks:** `ddl-create.test.ts` / `\"admin create is idempotent and conflicts are explicit\"`; `ddl-auth.test.ts` / `\"non-admin DDL is forbidden\"`.",
    "**AC-4 (add-column compatibility):** GIVEN existing rows and an add-column request using `null` or `default`, WHEN current rows are read after the atomic schema replacement, THEN absent fields resolve to the pinned null/default value, new non-nullable writes require the column, and source JSONL is not rewritten. **Checks:** `ddl-evolution.test.ts` / `\"null backfill is logical\"`; `\"default backfill is logical and new writes materialize\"`.",
    "**AC-5 (single-switch materialized migration and recovery):** GIVEN a permitted add/materialize or retype/materialize operation, WHEN every new-generation row validates and the immutable generation is fsynced, THEN the only reader-visible switch is atomic `schema.json` replacement to that new path, the old generation remains untouched, and the table schema version increments; WHEN a process crashes before that switch, THEN old schema/path/data remain live and recovery removes or quarantines only the unpublished generation; WHEN it crashes after that switch, THEN recovery verifies the new path/hash and finalizes or fails closed without an automatic rollback or overwrite of the old generation. **Checks:** `ddl-migration.test.ts` / `\"pre-switch crash preserves old schema and generation\"`, `\"schema switch exposes complete immutable generation\"`, `\"post-switch crash verifies and finalizes without rollback\"`, `\"conversion failure preserves old state\"`.",
    "**AC-6 (serializer fencing and concurrency):** GIVEN a DDL operation in its drain/fence phase and a concurrent write from another process, WHEN the writer attempts to flush, THEN it waits or receives the defined retryable serializer result and writes neither source nor replacement data; after DDL completion, a new write validates against exactly one schema version. **Checks:** `ddl-concurrency.test.ts` / `\"DDL fence excludes cross-process write\"`, `\"post-migration write uses new schema only\"`.",
    "**AC-7 (view re-declaration):** GIVEN a declared view and dependents, WHEN a compatible re-declare changes only the validated query while retaining the same exposed column names/types, THEN its version increments and dependent views validate; WHEN it drops/changes an exposed column or introduces a cycle/undeclared dependency, THEN it is rejected without replacing `schema.json`. **Checks:** `ddl-views.test.ts` / `\"compatible view redeclare increments version\"`, `\"dependency cycle and exposed-shape drift are rejected\"`.",
    "**AC-8 (bounded inference removal):** GIVEN the migration window is active and an operator creates a table with `inferFrom` pointing to one existing JSONL file, WHEN inference succeeds, THEN the generated declaration is shown for confirmation and persisted once; after the configured deadline or first successful declaration, all generic reads use declared types/headers and per-query `read_json_auto` inference is rejected with a migration warning. **Checks:** `ddl-inference-compat.test.ts` / `\"create-only inference requires confirmation\"`, `\"declared read never calls auto inference\"`, `\"expired compatibility window rejects inference\"`.",
    "**AC-9 (strict value semantics):** GIVEN writes and defaults for every declared v1 type, WHEN values are serialized and read through the generic table path, THEN canonical `int64`, timestamp, and bytes forms round-trip without precision loss, JSON null follows the declared nullable rule, and noncanonical strings/numbers/booleans or implicit coercions are rejected before enqueue. **Checks:** `schema-types.test.ts` / `\"int64 precision survives JavaScript boundary\"`, `\"timestamp and bytes normalize canonically\"`, `\"json null and nullable rule are explicit\"`, `\"no implicit primitive coercion\"`."
  ],
  "passed": true,
  "worktree": "/home/kara/duckbrain",
  "branch": "feat/native-s3",
  "commit": "a67512b6778bead03e16c74b2c3d4a6cad81f7a6",
  "items": [],
  "stages": {
    "tier1": {
      "id": "tier1",
      "passed": true,
      "any_failed": false,
      "summary": "  \u2713 secrets: \u001b[90m3:53AM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~8514611 bytes (8.51 MB) in 1.44s\u001b[0m\n\u001b[90m3:53AM\u001b[0m \u001b[32m\n  \u2713 tests: \n RUN  v4.1.10 /home/kara/duckbrain\n\n\n Test Files  144 passed (144)\n      Tests  1152 passed (1152)\n",
      "steps": [
        {
          "id": "secrets",
          "type": "script",
          "passed": true,
          "output": "\u001b[90m3:53AM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~8514611 bytes (8.51 MB) in 1.44s\u001b[0m\n\u001b[90m3:53AM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mno leaks found\u001b[0m\n",
          "error": "",
          "data": {
            "exit_code": 0
          }
        },
        {
          "id": "tests",
          "type": "script",
          "passed": true,
          "output": "\n RUN  v4.1.10 /home/kara/duckbrain\n\n\n Test Files  144 passed (144)\n      Tests  1152 passed (1152)\n   Start at  03:53:39\n   Duration  45.94s (transform 2.10s, setup 892ms, import 37.62s, tests 126.09s, environment 16ms)\n\n",
          "error": "",
          "data": {
            "exit_code": 0
          }
        }
      ]
    },
    "tier2": {
      "id": "tier2",
      "passed": true,
      "any_failed": false,
      "summary": "COMPLETE\n  \u2713 **AC-1 (persistent declaration and types):** GIVEN a valid v1 `schema.json` containing object and positional tables across every declared type, WHEN a fresh process loads the namespace, THEN it reconstructs the same declared validators, storage paths, keys, headers, canonical type representations, and table versions in `TableSchemaRegistry` without inferring a data line or coercing a value. **Checks:** `schema-registry.test.ts` / `\"loads v1 object and positional declarations after restart\"`; `schema-json.test.ts` / `\"rejects unknown fields and invalid declarations\"`; `schema-types.test.ts` / `\"declared types use canonical JSONL and API representations\"`.: All 3 named tests pass: schema-registry.test.ts:96 \"loads v1 object and positional declarations after restart\" (73ms) uses vi.resetModules() for a fresh process, asserts no data file exists, reconstructs storagePath/keyColumns/positional header/schemaVersion, and rejects int64 coercion (\"canonical base-10 int64 STRING\"); schema-json.test.ts:66 \"rejects unknown fields and invalid declarations\" (27ms) asserts closed-world rejection of unknown keys/fields, reserved names, bad key columns, noncanonical defaults; schema-types.test.ts:120 \"declared types use canonical JSONL and API representations\" (5ms). Implementation: src/serialization/schemaRegistry.ts loadNamespaceSchema registers into tableSchemaRegistry only after whole-document validation.\n  \u2713 **AC-2 (schema/manifest separation):** GIVEN a namespace with both metadata files, WHEN a table is created, a row is written, and a partition is added, THEN `schema.json` contains only logical declarations, `manifest.json` remains only partitions plus `lastUpdated`, and historical manifest reading in `src/git/asof.ts` remains compatible. **Checks:** `ddl-manifest-contract.test.ts` / `\"schema and manifest retain separate ownership\"`; `asof-ddl-compat.test.ts` / `\"historical manifest remains readable\"`.: ddl-manifest-contract.test.ts:63 \"schema and manifest retain separate ownership\" (136ms) asserts Object.keys(manifest).sort() === [\"lastUpdated\",\"partitions\"], that a non-conforming declared path adds no manifest entry, and schema.json holds only logical declarations. asof-ddl-compat.test.ts:78 \"historical manifest remains readable\" (124ms) builds a real git repo, runs a real createTable, commits it, and asserts readManifestAtRef at the pre-DDL ref returns exactly {partitions, lastUpdated} and readRowsAtRef resolves rows; schema.json never leaks into the partition index.\n  \u2713 **AC-3 (atomic authorized create):** GIVEN an admin and a non-admin principal, WHEN both attempt the same create-table request, THEN only the admin can create it; a retry with the same idempotency key and canonical request returns the original declaration, while a different declaration for that name is rejected without modifying either metadata file. **Checks:** `ddl-create.test.ts` / `\"admin create is idempotent and conflicts are explicit\"`; `ddl-auth.test.ts` / `\"non-admin DDL is forbidden\"`.: ddl-create.test.ts:70 \"admin create is idempotent and conflicts are explicit\" (146ms) asserts idempotentReplay===true, schema.json bytes unchanged after replay, exactly one extra journal, no data row created, and explicit 409 conflicts. ddl-auth.test.ts:57 \"non-admin DDL is forbidden\" (159ms) asserts writer/analyst/uploader and out-of-scope admin all get 403 FORBIDDEN with no schema.json and no .duckbrain-ddl dir created, and that addColumn/retypeColumn/declareView/redeclareView are refused for non-admins.\n  \u2713 **AC-4 (add-column compatibility):** GIVEN existing rows and an add-column request using `null` or `default`, WHEN current rows are read after the atomic schema replacement, THEN absent fields resolve to the pinned null/default value, new non-nullable writes require the column, and source JSONL is not rewritten. **Checks:** `ddl-evolution.test.ts` / `\"null backfill is logical\"`; `\"default backfill is logical and new writes materialize\"`.: ddl-evolution.test.ts:203 \"null backfill is logical\" (118ms) and :258 \"default backfill is logical and new writes materialize\" (155ms) pass. They assert sha256 of the source JSONL is byte-identical before/after addColumn, added.generation===undefined (logical policy), historical rows read back {note:null} / {tag:\"promoted\"}, a new non-nullable write without the column returns VALIDATION_ERROR, and reads go through the real buildSelectPlan/executeSelectPlan + namespaceWriter.enqueue paths.\n  \u2713 **AC-5 (single-switch materialized migration and recovery):** GIVEN a permitted add/materialize or retype/materialize operation, WHEN every new-generation row validates and the immutable generation is fsynced, THEN the only reader-visible switch is atomic `schema.json` replacement to that new path, the old generation remains untouched, and the table schema version increments; WHEN a process crashes before that switch, THEN old schema/path/data remain live and recovery removes or quarantines only the unpublished generation; WHEN it crashes after that switch, THEN recovery verifies the new path/hash and finalizes or fails closed without an automatic rollback or overwrite of the old generation. **Checks:** `ddl-migration.test.ts` / `\"pre-switch crash preserves old schema and generation\"`, `\"schema switch exposes complete immutable generation\"`, `\"post-switch crash verifies and finalizes without rollback\"`, `\"conversion failure preserves old state\"`.: All 4 named tests pass: ddl-migration.test.ts:222 \"pre-switch crash preserves old schema and generation\" (1231ms), :341 \"schema switch exposes complete immutable generation\" (409ms), :? \"post-switch crash verifies and finalizes without rollback\" (1608ms), :? \"conversion failure preserves old state\" (68ms). They spawn real child processes that SIGKILL at durability boundaries (exit code 97), then recover from a FRESH process, asserting schema.json/storage sha unchanged pre-switch, generation dir contents, version increment, \"abandoned\"/\"finalized\"/\"fail-closed\" recovery actions, and fail-closed on a tampered published-generation hash with no rollback.\n  \u2713 **AC-6 (serializer fencing and concurrency):** GIVEN a DDL operation in its drain/fence phase and a concurrent write from another process, WHEN the writer attempts to flush, THEN it waits or receives the defined retryable serializer result and writes neither source nor replacement data; after DDL completion, a new write validates against exactly one schema version. **Checks:** `ddl-concurrency.test.ts` / `\"DDL fence excludes cross-process write\"`, `\"post-migration write uses new schema only\"`.: ddl-concurrency.test.ts:207 \"DDL fence excludes cross-process write\" (778ms) spawns a real child that holds the namespace lock via onFenceAcquired; the parent's writer.enqueue returns {ok:false, code:\"SERIALIZER_LOCKED\", retryAfter>0}, and the test asserts the source sha is unchanged, the generation dir does not exist, and the blocked row appears in neither source nor replacement. :? \"post-migration write uses new schema only\" (105ms) asserts a post-migration write validates against exactly the new schema version. Implementation: src/serialization/ddl.ts:423 acquireDdlFence uses the existing cross-process namespace write lock.\n  \u2713 **AC-7 (view re-declaration):** GIVEN a declared view and dependents, WHEN a compatible re-declare changes only the validated query while retaining the same exposed column names/types, THEN its version increments and dependent views validate; WHEN it drops/changes an exposed column or introduces a cycle/undeclared dependency, THEN it is rejected without replacing `schema.json`. **Checks:** `ddl-views.test.ts` / `\"compatible view redeclare increments version\"`, `\"dependency cycle and exposed-shape drift are rejected\"`.: ddl-views.test.ts:63 \"compatible view redeclare increments version\" (170ms) asserts version 1->2, changed query persisted, exposed columns unchanged, dependent view still validates, and no storage path on a view. :? \"dependency cycle and exposed-shape drift are rejected\" (292ms) asserts VIEW_SHAPE_CHANGED 409 on dropped column, VALIDATION_ERROR on retype/cycle/undeclared dependency/undeclared column, VIEW_NOT_FOUND 404, VIEW_ALREADY_DECLARED, and that schema.json text is byte-identical after every rejection.\n  \u2713 **AC-8 (bounded inference removal):** GIVEN the migration window is active and an operator creates a table with `inferFrom` pointing to one existing JSONL file, WHEN inference succeeds, THEN the generated declaration is shown for confirmation and persisted once; after the configured deadline or first successful declaration, all generic reads use declared types/headers and per-query `read_json_auto` inference is rejected with a migration warning. **Checks:** `ddl-inference-compat.test.ts` / `\"create-only inference requires confirmation\"`, `\"declared read never calls auto inference\"`, `\"expired compatibility window rejects inference\"`.: All 3 named tests pass: ddl-inference-compat.test.ts:88 \"create-only inference requires confirmation\" (107ms) asserts status \"confirmation_required\" with the generated declaration shown and nothing persisted, then exactly one journal after confirm, plus KEY_COLUMNS_REQUIRED/INFERENCE_AMBIGUOUS refusals. :? \"declared read never calls auto inference\" (127ms) asserts the built SQL contains auto_detect=false and never read_json_auto/auto_detect=true, and schemaInferenceCompatCount()===0. :? \"expired compatibility window rejects inference\" (63ms) asserts 409 SCHEMA_DECLARATION_REQUIRED with \"inference is no longer permitted\" and that explicit declarations still work.\n  \u2713 **AC-9 (strict value semantics):** GIVEN writes and defaults for every declared v1 type, WHEN values are serialized and read through the generic table path, THEN canonical `int64`, timestamp, and bytes forms round-trip without precision loss, JSON null follows the declared nullable rule, and noncanonical strings/numbers/booleans or implicit coercions are rejected before enqueue. **Checks:** `schema-types.test.ts` / `\"int64 precision survives JavaScript boundary\"`, `\"timestamp and bytes normalize canonically\"`, `\"json null and nullable rule are explicit\"`, `\"no implicit primitive coercion\"`.: All 4 named tests pass: schema-types.test.ts:? \"int64 precision survives JavaScript boundary\" (42ms) reads 9223372036854775807/-9223372036854775808 back as exact strings through executeSelectPlan and rejects JSON numbers, noncanonical text (\"0123\",\"+42\",\"1e3\",\"1.0\"), and out-of-range values before enqueue. \"timestamp and bytes normalize canonically\" asserts UTC millisecond normalization and base64url envelope with rejection of date-only/local/epoch and unpadded/standard-base64 forms. \"json null and nullable rule are explicit\" asserts null only for nullable columns and verbatim RFC 8259 values. \"no implicit primitive coercion\" rejects number->string, number->int64, string->float64, string->boolean, epoch->timestamp, Buffer->bytes, and undeclared properties.\nAll 9 acceptance criteria are satisfied: every named test exists, is substantive (real filesystem, real SIGKILL child processes, real git repos, real DuckDB reads), and passes; the full configured suite `npx vitest run` reports 144 files / 1152 tests passed with 0 failures and 0 LSP diagnostics.",
      "steps": [
        {
          "id": "tier2",
          "type": "ai_eval",
          "passed": true,
          "output": "COMPLETE\n  \u2713 **AC-1 (persistent declaration and types):** GIVEN a valid v1 `schema.json` containing object and positional tables across every declared type, WHEN a fresh process loads the namespace, THEN it reconstructs the same declared validators, storage paths, keys, headers, canonical type representations, and table versions in `TableSchemaRegistry` without inferring a data line or coercing a value. **Checks:** `schema-registry.test.ts` / `\"loads v1 object and positional declarations after restart\"`; `schema-json.test.ts` / `\"rejects unknown fields and invalid declarations\"`; `schema-types.test.ts` / `\"declared types use canonical JSONL and API representations\"`.: All 3 named tests pass: schema-registry.test.ts:96 \"loads v1 object and positional declarations after restart\" (73ms) uses vi.resetModules() for a fresh process, asserts no data file exists, reconstructs storagePath/keyColumns/positional header/schemaVersion, and rejects int64 coercion (\"canonical base-10 int64 STRING\"); schema-json.test.ts:66 \"rejects unknown fields and invalid declarations\" (27ms) asserts closed-world rejection of unknown keys/fields, reserved names, bad key columns, noncanonical defaults; schema-types.test.ts:120 \"declared types use canonical JSONL and API representations\" (5ms). Implementation: src/serialization/schemaRegistry.ts loadNamespaceSchema registers into tableSchemaRegistry only after whole-document validation.\n  \u2713 **AC-2 (schema/manifest separation):** GIVEN a namespace with both metadata files, WHEN a table is created, a row is written, and a partition is added, THEN `schema.json` contains only logical declarations, `manifest.json` remains only partitions plus `lastUpdated`, and historical manifest reading in `src/git/asof.ts` remains compatible. **Checks:** `ddl-manifest-contract.test.ts` / `\"schema and manifest retain separate ownership\"`; `asof-ddl-compat.test.ts` / `\"historical manifest remains readable\"`.: ddl-manifest-contract.test.ts:63 \"schema and manifest retain separate ownership\" (136ms) asserts Object.keys(manifest).sort() === [\"lastUpdated\",\"partitions\"], that a non-conforming declared path adds no manifest entry, and schema.json holds only logical declarations. asof-ddl-compat.test.ts:78 \"historical manifest remains readable\" (124ms) builds a real git repo, runs a real createTable, commits it, and asserts readManifestAtRef at the pre-DDL ref return\n\u2026 [7268 chars omitted] \u2026\nor every declared v1 type, WHEN values are serialized and read through the generic table path, THEN canonical `int64`, timestamp, and bytes forms round-trip without precision loss, JSON null follows the declared nullable rule, and noncanonical strings/numbers/booleans or implicit coercions are rejected before enqueue. **Checks:** `schema-types.test.ts` / `\"int64 precision survives JavaScript boundary\"`, `\"timestamp and bytes normalize canonically\"`, `\"json null and nullable rule are explicit\"`, `\"no implicit primitive coercion\"`.: All 4 named tests pass: schema-types.test.ts:? \"int64 precision survives JavaScript boundary\" (42ms) reads 9223372036854775807/-9223372036854775808 back as exact strings through executeSelectPlan and rejects JSON numbers, noncanonical text (\"0123\",\"+42\",\"1e3\",\"1.0\"), and out-of-range values before enqueue. \"timestamp and bytes normalize canonically\" asserts UTC millisecond normalization and base64url envelope with rejection of date-only/local/epoch and unpadded/standard-base64 forms. \"json null and nullable rule are explicit\" asserts null only for nullable columns and verbatim RFC 8259 values. \"no implicit primitive coercion\" rejects number->string, number->int64, string->float64, string->boolean, epoch->timestamp, Buffer->bytes, and undeclared properties.\nAll 9 acceptance criteria are satisfied: every named test exists, is substantive (real filesystem, real SIGKILL child processes, real git repos, real DuckDB reads), and passes; the full configured suite `npx vitest run` reports 144 files / 1152 tests passed with 0 failures and 0 LSP diagnostics.",
          "error": "",
          "data": {
            "verdict": "COMPLETE",
            "items": [
              {
                "criterion": "**AC-1 (persistent declaration and types):** GIVEN a valid v1 `schema.json` containing object and positional tables across every declared type, WHEN a fresh process loads the namespace, THEN it reconstructs the same declared validators, storage paths, keys, headers, canonical type representations, and table versions in `TableSchemaRegistry` without inferring a data line or coercing a value. **Checks:** `schema-registry.test.ts` / `\"loads v1 object and positional declarations after restart\"`; `schema-json.test.ts` / `\"rejects unknown fields and invalid declarations\"`; `schema-types.test.ts` / `\"declared types use canonical JSONL and API representations\"`.",
                "status": "PASS",
                "detail": "All 3 named tests pass: schema-registry.test.ts:96 \"loads v1 object and positional declarations after restart\" (73ms) uses vi.resetModules() for a fresh process, asserts no data file exists, reconstructs storagePath/keyColumns/positional header/schemaVersion, and rejects int64 coercion (\"canonical base-10 int64 STRING\"); schema-json.test.ts:66 \"rejects unknown fields and invalid declarations\" (27ms) asserts closed-world rejection of unknown keys/fields, reserved names, bad key columns, noncanonical defaults; schema-types.test.ts:120 \"declared types use canonical JSONL and API representations\" (5ms). Implementation: src/serialization/schemaRegistry.ts loadNamespaceSchema registers into tableSchemaRegistry only after whole-document validation."
              },
              {
                "criterion": "**AC-2 (schema/manifest separation):** GIVEN a namespace with both metadata files, WHEN a table is created, a row is written, and a partition is added, THEN `schema.json` contains only logical declarations, `manifest.json` remains only partitions plus `lastUpdated`, and historical manifest reading in `src/git/asof.ts` remains compatible. **Checks:** `ddl-manifest-contract.test.ts` / `\"schema and manifest retain separate ownership\"`; `asof-ddl-compat.test.ts` / `\"historical manifest remains readable\"`.",
                "status": "PASS",
                "detail": "ddl-manifest-contract.test.ts:63 \"schema and manifest retain separate ownership\" (136ms) asserts Object.keys(manifest).sort() === [\"lastUpdated\",\"partitions\"], that a non-conforming declared path adds no manifest entry, and schema.json holds only logical declarations. asof-ddl-compat.test.ts:78 \"historical manifest remains readable\" (124ms) builds a real git repo, runs a real createTable, commits it, and asserts readManifestAtRef at the pre-DDL ref returns exactly {partitions, lastUpdated} and readRowsAtRef resolves rows; schema.json never leaks into the partition index."
              },
              {
                "criterion": "**AC-3 (atomic authorized create):** GIVEN an admin and a non-admin principal, WHEN both attempt the same create-table request, THEN only the admin can create it; a retry with the same idempotency key and canonical request returns the original declaration, while a different declaration for that name is rejected without modifying either metadata file. **Checks:** `ddl-create.test.ts` / `\"admin create is idempotent and conflicts are explicit\"`; `ddl-auth.test.ts` / `\"non-admin DDL is forbidden\"`.",
                "status": "PASS",
                "detail": "ddl-create.test.ts:70 \"admin create is idempotent and conflicts are explicit\" (146ms) asserts idempotentReplay===true, schema.json bytes unchanged after replay, exactly one extra journal, no data row created, and explicit 409 conflicts. ddl-auth.test.ts:57 \"non-admin DDL is forbidden\" (159ms) asserts writer/analyst/uploader and out-of-scope admin all get 403 FORBIDDEN with no schema.json and no .duckbrain-ddl dir created, and that addColumn/retypeColumn/declareView/redeclareView are refused for non-admins."
              },
              {
                "criterion": "**AC-4 (add-column compatibility):** GIVEN existing rows and an add-column request using `null` or `default`, WHEN current rows are read after the atomic schema replacement, THEN absent fields resolve to the pinned null/default value, new non-nullable writes require the column, and source JSONL is not rewritten. **Checks:** `ddl-evolution.test.ts` / `\"null backfill is logical\"`; `\"default backfill is logical and new writes materialize\"`.",
                "status": "PASS",
                "detail": "ddl-evolution.test.ts:203 \"null backfill is logical\" (118ms) and :258 \"default backfill is logical and new writes materialize\" (155ms) pass. They assert sha256 of the source JSONL is byte-identical before/after addColumn, added.generation===undefined (logical policy), historical rows read back {note:null} / {tag:\"promoted\"}, a new non-nullable write without the column returns VALIDATION_ERROR, and reads go through the real buildSelectPlan/executeSelectPlan + namespaceWriter.enqueue paths."
              },
              {
                "criterion": "**AC-5 (single-switch materialized migration and recovery):** GIVEN a permitted add/materialize or retype/materialize operation, WHEN every new-generation row validates and the immutable generation is fsynced, THEN the only reader-visible switch is atomic `schema.json` replacement to that new path, the old generation remains untouched, and the table schema version increments; WHEN a process crashes before that switch, THEN old schema/path/data remain live and recovery removes or quarantines only the unpublished generation; WHEN it crashes after that switch, THEN recovery verifies the new path/hash and finalizes or fails closed without an automatic rollback or overwrite of the old generation. **Checks:** `ddl-migration.test.ts` / `\"pre-switch crash preserves old schema and generation\"`, `\"schema switch exposes complete immutable generation\"`, `\"post-switch crash verifies and finalizes without rollback\"`, `\"conversion failure preserves old state\"`.",
                "status": "PASS",
                "detail": "All 4 named tests pass: ddl-migration.test.ts:222 \"pre-switch crash preserves old schema and generation\" (1231ms), :341 \"schema switch exposes complete immutable generation\" (409ms), :? \"post-switch crash verifies and finalizes without rollback\" (1608ms), :? \"conversion failure preserves old state\" (68ms). They spawn real child processes that SIGKILL at durability boundaries (exit code 97), then recover from a FRESH process, asserting schema.json/storage sha unchanged pre-switch, generation dir contents, version increment, \"abandoned\"/\"finalized\"/\"fail-closed\" recovery actions, and fail-closed on a tampered published-generation hash with no rollback."
              },
              {
                "criterion": "**AC-6 (serializer fencing and concurrency):** GIVEN a DDL operation in its drain/fence phase and a concurrent write from another process, WHEN the writer attempts to flush, THEN it waits or receives the defined retryable serializer result and writes neither source nor replacement data; after DDL completion, a new write validates against exactly one schema version. **Checks:** `ddl-concurrency.test.ts` / `\"DDL fence excludes cross-process write\"`, `\"post-migration write uses new schema only\"`.",
                "status": "PASS",
                "detail": "ddl-concurrency.test.ts:207 \"DDL fence excludes cross-process write\" (778ms) spawns a real child that holds the namespace lock via onFenceAcquired; the parent's writer.enqueue returns {ok:false, code:\"SERIALIZER_LOCKED\", retryAfter>0}, and the test asserts the source sha is unchanged, the generation dir does not exist, and the blocked row appears in neither source nor replacement. :? \"post-migration write uses new schema only\" (105ms) asserts a post-migration write validates against exactly the new schema version. Implementation: src/serialization/ddl.ts:423 acquireDdlFence uses the existing cross-process namespace write lock."
              },
              {
                "criterion": "**AC-7 (view re-declaration):** GIVEN a declared view and dependents, WHEN a compatible re-declare changes only the validated query while retaining the same exposed column names/types, THEN its version increments and dependent views validate; WHEN it drops/changes an exposed column or introduces a cycle/undeclared dependency, THEN it is rejected without replacing `schema.json`. **Checks:** `ddl-views.test.ts` / `\"compatible view redeclare increments version\"`, `\"dependency cycle and exposed-shape drift are rejected\"`.",
                "status": "PASS",
                "detail": "ddl-views.test.ts:63 \"compatible view redeclare increments version\" (170ms) asserts version 1->2, changed query persisted, exposed columns unchanged, dependent view still validates, and no storage path on a view. :? \"dependency cycle and exposed-shape drift are rejected\" (292ms) asserts VIEW_SHAPE_CHANGED 409 on dropped column, VALIDATION_ERROR on retype/cycle/undeclared dependency/undeclared column, VIEW_NOT_FOUND 404, VIEW_ALREADY_DECLARED, and that schema.json text is byte-identical after every rejection."
              },
              {
                "criterion": "**AC-8 (bounded inference removal):** GIVEN the migration window is active and an operator creates a table with `inferFrom` pointing to one existing JSONL file, WHEN inference succeeds, THEN the generated declaration is shown for confirmation and persisted once; after the configured deadline or first successful declaration, all generic reads use declared types/headers and per-query `read_json_auto` inference is rejected with a migration warning. **Checks:** `ddl-inference-compat.test.ts` / `\"create-only inference requires confirmation\"`, `\"declared read never calls auto inference\"`, `\"expired compatibility window rejects inference\"`.",
                "status": "PASS",
                "detail": "All 3 named tests pass: ddl-inference-compat.test.ts:88 \"create-only inference requires confirmation\" (107ms) asserts status \"confirmation_required\" with the generated declaration shown and nothing persisted, then exactly one journal after confirm, plus KEY_COLUMNS_REQUIRED/INFERENCE_AMBIGUOUS refusals. :? \"declared read never calls auto inference\" (127ms) asserts the built SQL contains auto_detect=false and never read_json_auto/auto_detect=true, and schemaInferenceCompatCount()===0. :? \"expired compatibility window rejects inference\" (63ms) asserts 409 SCHEMA_DECLARATION_REQUIRED with \"inference is no longer permitted\" and that explicit declarations still work."
              },
              {
                "criterion": "**AC-9 (strict value semantics):** GIVEN writes and defaults for every declared v1 type, WHEN values are serialized and read through the generic table path, THEN canonical `int64`, timestamp, and bytes forms round-trip without precision loss, JSON null follows the declared nullable rule, and noncanonical strings/numbers/booleans or implicit coercions are rejected before enqueue. **Checks:** `schema-types.test.ts` / `\"int64 precision survives JavaScript boundary\"`, `\"timestamp and bytes normalize canonically\"`, `\"json null and nullable rule are explicit\"`, `\"no implicit primitive coercion\"`.",
                "status": "PASS",
                "detail": "All 4 named tests pass: schema-types.test.ts:? \"int64 precision survives JavaScript boundary\" (42ms) reads 9223372036854775807/-9223372036854775808 back as exact strings through executeSelectPlan and rejects JSON numbers, noncanonical text (\"0123\",\"+42\",\"1e3\",\"1.0\"), and out-of-range values before enqueue. \"timestamp and bytes normalize canonically\" asserts UTC millisecond normalization and base64url envelope with rejection of date-only/local/epoch and unpadded/standard-base64 forms. \"json null and nullable rule are explicit\" asserts null only for nullable columns and verbatim RFC 8259 values. \"no implicit primitive coercion\" rejects number->string, number->int64, string->float64, string->boolean, epoch->timestamp, Buffer->bytes, and undeclared properties."
              }
            ],
            "summary": "All 9 acceptance criteria are satisfied: every named test exists, is substantive (real filesystem, real SIGKILL child processes, real git repos, real DuckDB reads), and passes; the full configured suite `npx vitest run` reports 144 files / 1152 tests passed with 0 failures and 0 LSP diagnostics."
          }
        }
      ]
    }
  },
  "summary": "Judge Result: DB-SUPA-6\n\nStage tier1: PASS\n    \u2713 secrets: \u001b[90m3:53AM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~8514611 bytes (8.51 MB) in 1.44s\u001b[0m\n\u001b[90m3:53AM\u001b[0m \u001b[32m\n  \u2713 tests: \n RUN  v4.1.10 /home/kara/duckbrain\n\n\n Test Files  144 passed (144)\n      Tests  1152 passed (1152)\n\n\nStage tier2: PASS\n  COMPLETE\n  \u2713 **AC-1 (persistent declaration and types):** GIVEN a valid v1 `schema.json` containing object and positional tables across every declared type, WHEN a fresh process loads the namespace, THEN it reconstructs the same declared validators, storage paths, keys, headers, canonical type representations, and table versions in `TableSchemaRegistry` without inferring a data line or coercing a value. **Checks:** `schema-registry.test.ts` / `\"loads v1 object and positional declarations after restart\"`; `schema-json.test.ts` / `\"rejects unknown fields and invalid declarations\"`; `schema-types.test.ts` / `\"declared types use canonical JSONL and API representations\"`.: All 3 named tests pass: schema-registry.test.ts:96 \"loads v1 object and positional declarations after restart\" (73ms) uses vi.resetModules() for a fresh process, asserts no data file exists, reconstructs storagePath/keyColumns/positional header/schemaVersion, and rejects int64 coercion (\"canonical base-10 int64 STRING\"); schema-json.test.ts:66 \"rejects unknown fields and invalid declarations\" (27ms) asserts closed-world rejection of unknown keys/fields, reserved names, bad key columns, noncanonical defaults; schema-types.test.ts:120 \"declared types use canonical JSONL and API representations\" (5ms). Implementation: src/serialization/schemaRegistry.ts loadNamespaceSchema registers into tableSchemaRegistry only after whole-document validation.\n  \u2713 **AC-2 (schema/manifest separation):** GIVEN a namespace with both metadata files, WHEN a table is created, a row is written, and a partition is added, THEN `schema.json` contains only logical declarations, `manifest.json` remains only partitions plus `lastUpdated`, and historical manifest reading in `src/git/asof.ts` remains compatible. **Checks:** `ddl-manifest-contract.test.ts` / `\"schema and manifest retain separate ownership\"`; `asof-ddl-compat.test.ts` / `\"historical manifest remains readable\"`.: ddl-manifest-contract.test.ts:63 \"schema and manifest retain separate ownership\" (136ms) asserts Object.keys(manifest).sort() === [\"lastUpdated\",\"partitions\"], that a non-conforming declared path adds no manifest entry, and schema.json holds only logical declarations. asof-ddl-compat.test.ts:78 \"historical manifest remains readable\" (124ms) builds a real git repo, runs a real createTable, commits it, and asserts readManifestAtRef at the pre-DDL ref returns exactly {partitions, lastUpdated} and readRowsAtRef resolves rows; schema.json never leaks into the partition index.\n  \u2713 **AC-3 (atomic authorized create):** GIVEN an admin and a non-admin principal, WHEN both attempt the same create-table request, THEN only the admin can create it; a retry with the same idempotency key and canonical request returns the original declaration, while a different declaration for that name is rejected without modifying either metadata file. **Checks:** `ddl-create.test.ts` / `\"admin create is idempotent and conflicts are explicit\"`; `ddl-auth.test.ts` / `\"non-admin DDL is forbidden\"`.: ddl-create.test.ts:70 \"admin create is idempotent and conflicts are explicit\" (146ms) asserts idempotentReplay===true, schema.json bytes unchanged after replay, exactly one extra journal, no data row created, and explicit 409 conflicts. ddl-auth.test.ts:57 \"non-admin DDL is forbidden\" (159ms) asserts writer/analyst/uploader and out-of-scope admin all get 403 FORBIDDEN with no schema.json and no .duckbrain-ddl dir created, and that addColumn/retypeColumn/declareView/redeclareView are refused for non-admins.\n  \u2713 **AC-4 (add-column compatibility):** GIVEN existing rows and an add-column request using `null` or `default`, WHEN current rows are read after the atomic schema replacement, THEN absent fields resolve to the pinned null/default value, new non-nullable writes require the column, and source JSONL is not rewritten. **Checks:** `ddl-evolution.test.ts` / `\"null backfill is logical\"`; `\"default backfill is logical and new writes materialize\"`.: ddl-evolution.test.ts:203 \"null backfill is logical\" (118ms) and :258 \"default backfill is logical and new writes materialize\" (155ms) pass. They assert sha256 of the source JSONL is byte-identical before/after addColumn, added.generation===undefined (logical policy), historical rows read back {note:null} / {tag:\"promoted\"}, a new non-nullable write without the column returns VALIDATION_ERROR, and reads go through the real buildSelectPlan/executeSelectPlan + namespaceWriter.enqueue paths.\n  \u2713 **AC-5 (single-switch materialized migration and recovery):** GIVEN a permitted add/materialize or retype/materialize operation, WHEN every new-generation row validates and the immutable generation is fsynced, THEN the only reader-visible switch is atomic `schema.json` replacement to that new path, the old generation remains untouched, and the table schema version increments; WHEN a process crashes before that switch, THEN old schema/path/data remain live and recovery removes or quarantines only the unpublished generation; WHEN it crashes after that switch, THEN recovery verifies the new path/hash and finalizes or fails closed without an automatic rollback or overwrite of the old generation. **Checks:** `ddl-migration.test.ts` / `\"pre-switch crash preserves old schema and generation\"`, `\"schema switch exposes complete immutable generation\"`, `\"post-switch crash verifies and finalizes without rollback\"`, `\"conversion failure preserves old state\"`.: All 4 named tests pass: ddl-migration.test.ts:222 \"pre-switch crash preserves old schema and generation\" (1231ms), :341 \"schema switch exposes complete immutable generation\" (409ms), :? \"post-switch crash verifies and finalizes without rollback\" (1608ms), :? \"conversion failure preserves old state\" (68ms). They spawn real child processes that SIGKILL at durability boundaries (exit code 97), then recover from a FRESH process, asserting schema.json/storage sha unchanged pre-switch, generation dir contents, version increment, \"abandoned\"/\"finalized\"/\"fail-closed\" recovery actions, and fail-closed on a tampered published-generation hash with no rollback.\n  \u2713 **AC-6 (serializer fencing and concurrency):** GIVEN a DDL operation in its drain/fence phase and a concurrent write from another process, WHEN the writer attempts to flush, THEN it waits or receives the defined retryable serializer result and writes neither source nor replacement data; after DDL completion, a new write validates against exactly one schema version. **Checks:** `ddl-concurrency.test.ts` / `\"DDL fence excludes cross-process write\"`, `\"post-migration write uses new schema only\"`.: ddl-concurrency.test.ts:207 \"DDL fence excludes cross-process write\" (778ms) spawns a real child that holds the namespace lock via onFenceAcquired; the parent's writer.enqueue returns {ok:false, code:\"SERIALIZER_LOCKED\", retryAfter>0}, and the test asserts the source sha is unchanged, the generation dir does not exist, and the blocked row appears in neither source nor replacement. :? \"post-migration write uses new schema only\" (105ms) asserts a post-migration write validates against exactly the new schema version. Implementation: src/serialization/ddl.ts:423 acquireDdlFence uses the existing cross-process namespace write lock.\n  \u2713 **AC-7 (view re-declaration):** GIVEN a declared view and dependents, WHEN a compatible re-declare changes only the validated query while retaining the same exposed column names/types, THEN its version increments and dependent views validate; WHEN it drops/changes an exposed column or introduces a cycle/undeclared dependency, THEN it is rejected without replacing `schema.json`. **Checks:** `ddl-views.test.ts` / `\"compatible view redeclare increments version\"`, `\"dependency cycle and exposed-shape drift are rejected\"`.: ddl-views.test.ts:63 \"compatible view redeclare increments version\" (170ms) asserts version 1->2, changed query persisted, exposed columns unchanged, dependent view still validates, and no storage path on a view. :? \"dependency cycle and exposed-shape drift are rejected\" (292ms) asserts VIEW_SHAPE_CHANGED 409 on dropped column, VALIDATION_ERROR on retype/cycle/undeclared dependency/undeclared column, VIEW_NOT_FOUND 404, VIEW_ALREADY_DECLARED, and that schema.json text is byte-identical after every rejection.\n  \u2713 **AC-8 (bounded inference removal):** GIVEN the migration window is active and an operator creates a table with `inferFrom` pointing to one existing JSONL file, WHEN inference succeeds, THEN the generated declaration is shown for confirmation and persisted once; after the configured deadline or first successful declaration, all generic reads use declared types/headers and per-query `read_json_auto` inference is rejected with a migration warning. **Checks:** `ddl-inference-compat.test.ts` / `\"create-only inference requires confirmation\"`, `\"declared read never calls auto inference\"`, `\"expired compatibility window rejects inference\"`.: All 3 named tests pass: ddl-inference-compat.test.ts:88 \"create-only inference requires confirmation\" (107ms) asserts status \"confirmation_required\" with the generated declaration shown and nothing persisted, then exactly one journal after confirm, plus KEY_COLUMNS_REQUIRED/INFERENCE_AMBIGUOUS refusals. :? \"declared read never calls auto inference\" (127ms) asserts the built SQL contains auto_detect=false and never read_json_auto/auto_detect=true, and schemaInferenceCompatCount()===0. :? \"expired compatibility window rejects inference\" (63ms) asserts 409 SCHEMA_DECLARATION_REQUIRED with \"inference is no longer permitted\" and that explicit declarations still work.\n  \u2713 **AC-9 (strict value semantics):** GIVEN writes and defaults for every declared v1 type, WHEN values are serialized and read through the generic table path, THEN canonical `int64`, timestamp, and bytes forms round-trip without precision loss, JSON null follows the declared nullable rule, and noncanonical strings/numbers/booleans or implicit coercions are rejected before enqueue. **Checks:** `schema-types.test.ts` / `\"int64 precision survives JavaScript boundary\"`, `\"timestamp and bytes normalize canonically\"`, `\"json null and nullable rule are explicit\"`, `\"no implicit primitive coercion\"`.: All 4 named tests pass: schema-types.test.ts:? \"int64 precision survives JavaScript boundary\" (42ms) reads 9223372036854775807/-9223372036854775808 back as exact strings through executeSelectPlan and rejects JSON numbers, noncanonical text (\"0123\",\"+42\",\"1e3\",\"1.0\"), and out-of-range values before enqueue. \"timestamp and bytes normalize canonically\" asserts UTC millisecond normalization and base64url envelope with rejection of date-only/local/epoch and unpadded/standard-base64 forms. \"json null and nullable rule are explicit\" asserts null only for nullable columns and verbatim RFC 8259 values. \"no implicit primitive coercion\" rejects number->string, number->int64, string->float64, string->boolean, epoch->timestamp, Buffer->bytes, and undeclared properties.\nAll 9 acceptance criteria are satisfied: every named test exists, is substantive (real filesystem, real SIGKILL child processes, real git repos, real DuckDB reads), and passes; the full configured suite `npx vitest run` reports 144 files / 1152 tests passed with 0 failures and 0 LSP diagnostics.\n\nOverall: PASS \u2713",
  "evaluated_at": "2026-09-17T08:59:15.605713"
}