diff --git a/.gitreins/config.yaml b/.gitreins/config.yaml
index ce5553f..24fcca3 100644
--- a/.gitreins/config.yaml
+++ b/.gitreins/config.yaml
@@ -16,7 +16,10 @@ guards:
 
 evaluator:
   max_iterations: 250
-  max_time: "45m"
+  # 45m starved CI-005's tier-2 on 2026-09-24: tier1 PASS + INCOMPLETE "Time cap
+  # (45m) exceeded" after the judge queued ~40m behind 12 concurrent fleet
+  # workers. 60m rung: cap now covers queue wait, not just evaluation.
+  max_time: "60m"
   # 4M starved DF-0919-01's tier-2 (the judge's clean-install reproduce needs
   # a wide exploration loop on this repo). 10M = the gitreins default.
   max_input_tokens: "10M"
diff --git a/.gitreins/tasks.yaml b/.gitreins/tasks.yaml
index cef799c..a99772b 100644
--- a/.gitreins/tasks.yaml
+++ b/.gitreins/tasks.yaml
@@ -1839,3 +1839,14 @@ tasks:
   status: complete
   created_at: '2026-09-23T07:13:22.549036+00:00'
   completed_at: '2026-09-23T07:52:54.042819+00:00'
+- id: CI-005
+  title: 'CI red recurrence: AGENTS.md test-count assertion fails on every run - derive
+    counts via sync script instead of hand-typed literals'
+  criteria:
+  - 'CI step ''Assert AGENTS.md test counts match live suite'' passes: counts in AGENTS.md
+    equal live vitest totals, extracted by a single shared script (scripts/sync-agents-md-counts.sh)
+    used by both the sync and the CI check mode; script is idempotent and fails loudly
+    on unparseable output'
+  status: complete
+  created_at: '2026-09-24T12:26:27.236778+00:00'
+  completed_at: '2026-09-24T12:39:32.307616+00:00'
