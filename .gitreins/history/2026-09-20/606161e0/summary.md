# Verdict: OPS-008

**Task:** OPS-008 semantic recall latency: bound on-the-fly embedding concurrency so a cache-miss ?q= stays inside the 30s budget
**Evaluated:** 2026-09-20T14:05:59.714806
**Result:** ✗ FAIL

## Pipeline Stages

- ✓ **tier1**
  -   ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: RUN  v5.0.1 /home/kara/duckbrain
- ✗ **tier2**
  - INCOMPLETE
  ✗ Measured: provider round-trip 2.3-7.6s (healthy), daemon ?q= 28-40s consistent incl. repeat query; attribution = serial on-the-fly embeds capped at 10 in recall.ts:371 x provider RTT. AC1: search.ts batches missing-vector embeds with bounded concurrency (reuse rebuild.ts worker-pool pattern) instead of a serial await in the scan loop. AC2: a warm-cache query is unchanged in behaviour (same ranking, same minScore floor). AC3: regression test asserts N cache-miss candidates are embedded with concurrency>1 (no serial 10x RTT wall) and that a single failing embed no longer stalls the rest of the batch. AC4: unit suite + tsc + prettier clean. AC5: live re-measure of authed GET /api/memories?namespace=duckbrain&q=... shows a materially lower wall time than the 28-40s baseline, documented in the tick event.: AC1 PASS: src/embedding/search.ts:131-154 implements the rebuild.ts worker-pool shape (shared nextIdx, N workers, Promise.all) — identical to src/embedding/rebuild.ts:122-156; Pass 1 (search.ts:141-166) resolves cache hits and collects deduped misses instead of the old serial `await provider.embed(text)` in the scan loop (old code confirmed via `git show d544776^:src/embedding/search.ts`). AC2 PASS: warm-cache test at src/embedding/search-concurrency.test.ts:243-268 asserts unchanged scores/order; minScore floor preserved at search.ts:205-214 (same inclusive `score >= minScore`). AC3 PASS: 10-test suite; RED control independently reproduced — reverting search.ts to d544776^ makes 3 tests fail (`expected 1 to be greater than 1` at test:136, and `expected 804 to be less than 500` at test:291 = serial 8x100ms), while the new code passes 10/10; failure isolation asserted at test:207-229. AC4 PASS: `npx vitest run` => 160 files / 1275 tests passed, exit 0; `npx tsc --noEmit` exit 0; `npx prettier --check` on the 3 changed files => 'All matched files use Prettier code style!', exit 0. AC5 FAIL: no tick event documents a post-fix live re-measure. HEAD is d544776 (2026-09-20 08:59:55) and `git log d544776..HEAD -- .coding-hermes/` is empty; the last board event is id 580 @ 08:05 and board.jsonl last_tick is 578 @ 07:58 — i.e. the fix commit has no accompanying tick event. The commit message contains only the BASELINE numbers (32.6/39.5/32.7s, 27.7s repeat), not a post-fix wall time, and .coding-hermes/board/tasks.jsonl OPS-008 still records the semantic ?q= re-measure as 'still pending'. No post-fix daemon ?q= measurement exists anywhere in .gitreins/ or .coding-hermes/.
AC1-AC4 are fully met with hard evidence (pooled embeds, unchanged warm-cache behaviour, RED-verified regression suite, 1275 tests + tsc + prettier clean), but AC5 fails: no post-fix live re-measure of the authed ?q= endpoint is documented in any tick event.

## Summary

Judge Result: OPS-008

Stage tier1: PASS
    ✓ secrets: secrets: harness state excluded from gitleaks scope (.gitreins/**)
  ✓ tests: RUN  v5.0.1 /home/kara/duckbrain

Stage tier2: FAIL
  INCOMPLETE
  ✗ Measured: provider round-trip 2.3-7.6s (healthy), daemon ?q= 28-40s consistent incl. repeat query; attribution = serial on-the-fly embeds capped at 10 in recall.ts:371 x provider RTT. AC1: search.ts batches missing-vector embeds with bounded concurrency (reuse rebuild.ts worker-pool pattern) instead of a serial await in the scan loop. AC2: a warm-cache query is unchanged in behaviour (same ranking, same minScore floor). AC3: regression test asserts N cache-miss candidates are embedded with concurrency>1 (no serial 10x RTT wall) and that a single failing embed no longer stalls the rest of the batch. AC4: unit suite + tsc + prettier clean. AC5: live re-measure of authed GET /api/memories?namespace=duckbrain&q=... shows a materially lower wall time than the 28-40s baseline, documented in the tick event.: AC1 PASS: src/embedding/search.ts:131-154 implements the rebuild.ts worker-pool shape (shared nextIdx, N workers, Promise.all) — identical to src/embedding/rebuild.ts:122-156; Pass 1 (search.ts:141-166) resolves cache hits and collects deduped misses instead of the old serial `await provider.embed(text)` in the scan loop (old code confirmed via `git show d544776^:src/embedding/search.ts`). AC2 PASS: warm-cache test at src/embedding/search-concurrency.test.ts:243-268 asserts unchanged scores/order; minScore floor preserved at search.ts:205-214 (same inclusive `score >= minScore`). AC3 PASS: 10-test suite; RED control independently reproduced — reverting search.ts to d544776^ makes 3 tests fail (`expected 1 to be greater than 1` at test:136, and `expected 804 to be less than 500` at test:291 = serial 8x100ms), while the new code passes 10/10; failure isolation asserted at test:207-229. AC4 PASS: `npx vitest run` => 160 files / 1275 tests passed, exit 0; `npx tsc --noEmit` exit 0; `npx prettier --check` on the 3 changed files => 'All matched files use Prettier code style!', exit 0. AC5 FAIL: no tick event documents a post-fix live re-measure. HEAD is d544776 (2026-09-20 08:59:55) and `git log d544776..HEAD -- .coding-hermes/` is empty; the last board event is id 580 @ 08:05 and board.jsonl last_tick is 578 @ 07:58 — i.e. the fix commit has no accompanying tick event. The commit message contains only the BASELINE numbers (32.6/39.5/32.7s, 27.7s repeat), not a post-fix wall time, and .coding-hermes/board/tasks.jsonl OPS-008 still records the semantic ?q= re-measure as 'still pending'. No post-fix daemon ?q= measurement exists anywhere in .gitreins/ or .coding-hermes/.
AC1-AC4 are fully met with hard evidence (pooled embeds, unchanged warm-cache behaviour, RED-verified regression suite, 1275 tests + tsc + prettier clean), but AC5 fails: no post-fix live re-measure of the authed ?q= endpoint is documented in any tick event.

Overall: FAIL ✗
