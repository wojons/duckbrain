# Verdict: DB-SUPA-2

**Task:** SUPA serialization layer
**Evaluated:** 2026-09-11T01:57:54.340585
**Result:** ✓ PASS

## Pipeline Stages

- ✓ **tier1**
  -   ✓ secrets: [90m8:48PM[0m [32mINF[0m [1mscanned ~7799576 bytes (7.80 MB) in 4.99s[0m
[90m8:48PM[0m [32m
  ✓ tests: 
 RUN  v4.1.10 /home/kara/duckbrain


 Test Files  110 passed (110)
      Tests  909 passed (909)
  
- ✓ **tier2**
  - COMPLETE
  ✓ Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes: src/serialization/namespaceWriter.ts: enqueue() serializes seq assignment + queue push under enqueueMutex (lines 322-402); flushOnce() splices the whole queue under the same mutex and sorts by request.seq (lines 670-680); appendFsyncBatch (lines 183-240) opens one fd per target and does a single writeSync(line+"\n") per record, so interleaving is impossible. Independent stress test I wrote and ran (src/__eval__/supa2-stress.test.ts, since removed): 8 namespaces x 200 concurrent enqueues = 1600 writes -> 'Test Files 1 passed (1) / Tests 1 passed (1)'; asserted all 1600 results ok:true, every line JSON.parse-able (throws on torn line), exactly 200 keys per namespace, no duplicates. Repo tests namespaceWriter.test.ts '32 writers across 4 namespaces leave parseable files and no lost acknowledgements' and cli/http-serialization.test.ts 'acknowledges 32 concurrent writes across 4 namespaces with parseable, complete files' (all 32 responses 201) pass. Full suite `npx vitest run` -> 'Test Files 110 passed (110) / Tests 909 passed (909)', exit 0.
  ✓ Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery: One writer per namespace: getNamespaceWriter() memoizes in a module Map keyed `${root}\0${ns}` (namespaceWriter.ts 862-874); all production paths route through it (remember.ts:210, duckdb/queries.ts:667 insertMemoryToPartition, HTTP POST/PUT). Deterministic order: ++this.nextSeq under enqueueMutex (line 382) + sort by seq in flushOnce (672-674); namespaceWriter.test.ts 'assigns monotonic seq and flushes data in seq order' asserts [1,2,3] and on-disk [0,1,2]. Cross-process lock: src/serialization/lock.ts acquireNamespaceWriteLock uses fs.openSync(lockPath,'wx',0o600) atomic O_EXCL with {pid,ts,nonce}; lock.test.ts 'has exactly one winner among 16 concurrent wx contenders' asserts exactly 1 winner. Dead-holder recovery: shouldBreak() returns true when !isPidAlive(payload.pid) (lock.ts:55) or lock age > LOCK_STALE_MS (10 min); lock.test.ts 'breaks a dead-pid lock immediately'. Fencing: tokenStillCurrent() re-read before each append (assertCurrent, lines 722-729) sets isFenced on mismatch; releaseNamespaceWriteLock only unlinks its own token. Independent cross-process test I wrote and ran (src/__eval__/xproc.test.ts, since removed): child acked a write then held the lock; contender got {ok:false, code:'SERIALIZER_LOCKED'} with file == ['/child/a']; after SIGKILL of the child the contender's next write succeeded and file == ['/child/a','/parent/acked'] -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. Repo test src/serialization/twowriter-kill9.test.ts covers the same scenario and passes in the full run.
  ✓ Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence: (a) Authz before enqueue: namespaceWriter.ts enqueue() calls checkAuthorization(provisional,'enqueue') at line 352 BEFORE the queue push at line 384; denial returns FORBIDDEN and never pushes (audit.test.ts 'writes enqueue denials without entering the data queue' asserts pendingRows===0). Re-checked at flush under the lock (line 741, phase 'flush'). HTTP maps FORBIDDEN->403 (src/http/routes/memories.ts:91-92); http-serialization.test.ts 'maps role denial to 403 without writing data and persists a denial audit' asserts 403 + zero data lines + denial audit row. (b) WAL before ack: flushOnce resolves pending promises only AFTER appendFsyncBatch/appendDirectBatch complete (lines 795-800); appendFsyncBatch does writeSync then fdatasyncSync per file (183-240); appendJsonlDurable (src/storage/durability.ts:307-338) does openSync/writeSync/barrierFile(fdatasyncSync)/closeSync + directory fsync before returning. Independent test I wrote and ran (src/__eval__/wal.test.ts, since removed): monkey-patched fs.fdatasyncSync to count barriers, enqueued one write into an fsync-mode namespace, asserted ack resolved with barriers>=1 and record on disk -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. (c) Audit evidence: acceptedAudit() emits {ts,ns,table,op,principal,outcome:'accepted',seq} to <ns>/_audit/current.jsonl (lines 776-782); deniedAudit() emits outcome:'denied'+reason. Independent test I wrote and ran (src/__eval__/audit.test.ts, since removed) asserted accepted rows carry principal alice/bob with seq 1/2 and a denied write yields outcome 'denied'/reason 'role'/principal 'mallory' with zero data rows -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. Audit rows use the buffered path after the data append (documented bounded crash window, lines 790-794) but are staged into git by `git add -A` in immediateCommit (src/git/autocommit.ts:95), so accepted writes retain durable audit evidence in namespace history. Full suite 110 files / 909 tests passed, exit 0; `npx tsc --noEmit` exit 0; LSP diagnostics 0.
All three SUPA-2 criteria verified: per-namespace single-writer fan-in with cross-process locking/fencing/dead-pid recovery, WAL-before-ack in durable modes, pre-enqueue authorization and retained audit evidence — confirmed by the repo's 909-test suite plus four independent tests I wrote and ran.

## Summary

Judge Result: DB-SUPA-2

Stage tier1: PASS
    ✓ secrets: [90m8:48PM[0m [32mINF[0m [1mscanned ~7799576 bytes (7.80 MB) in 4.99s[0m
[90m8:48PM[0m [32m
  ✓ tests: 
 RUN  v4.1.10 /home/kara/duckbrain


 Test Files  110 passed (110)
      Tests  909 passed (909)
  

Stage tier2: PASS
  COMPLETE
  ✓ Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes: src/serialization/namespaceWriter.ts: enqueue() serializes seq assignment + queue push under enqueueMutex (lines 322-402); flushOnce() splices the whole queue under the same mutex and sorts by request.seq (lines 670-680); appendFsyncBatch (lines 183-240) opens one fd per target and does a single writeSync(line+"\n") per record, so interleaving is impossible. Independent stress test I wrote and ran (src/__eval__/supa2-stress.test.ts, since removed): 8 namespaces x 200 concurrent enqueues = 1600 writes -> 'Test Files 1 passed (1) / Tests 1 passed (1)'; asserted all 1600 results ok:true, every line JSON.parse-able (throws on torn line), exactly 200 keys per namespace, no duplicates. Repo tests namespaceWriter.test.ts '32 writers across 4 namespaces leave parseable files and no lost acknowledgements' and cli/http-serialization.test.ts 'acknowledges 32 concurrent writes across 4 namespaces with parseable, complete files' (all 32 responses 201) pass. Full suite `npx vitest run` -> 'Test Files 110 passed (110) / Tests 909 passed (909)', exit 0.
  ✓ Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery: One writer per namespace: getNamespaceWriter() memoizes in a module Map keyed `${root}\0${ns}` (namespaceWriter.ts 862-874); all production paths route through it (remember.ts:210, duckdb/queries.ts:667 insertMemoryToPartition, HTTP POST/PUT). Deterministic order: ++this.nextSeq under enqueueMutex (line 382) + sort by seq in flushOnce (672-674); namespaceWriter.test.ts 'assigns monotonic seq and flushes data in seq order' asserts [1,2,3] and on-disk [0,1,2]. Cross-process lock: src/serialization/lock.ts acquireNamespaceWriteLock uses fs.openSync(lockPath,'wx',0o600) atomic O_EXCL with {pid,ts,nonce}; lock.test.ts 'has exactly one winner among 16 concurrent wx contenders' asserts exactly 1 winner. Dead-holder recovery: shouldBreak() returns true when !isPidAlive(payload.pid) (lock.ts:55) or lock age > LOCK_STALE_MS (10 min); lock.test.ts 'breaks a dead-pid lock immediately'. Fencing: tokenStillCurrent() re-read before each append (assertCurrent, lines 722-729) sets isFenced on mismatch; releaseNamespaceWriteLock only unlinks its own token. Independent cross-process test I wrote and ran (src/__eval__/xproc.test.ts, since removed): child acked a write then held the lock; contender got {ok:false, code:'SERIALIZER_LOCKED'} with file == ['/child/a']; after SIGKILL of the child the contender's next write succeeded and file == ['/child/a','/parent/acked'] -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. Repo test src/serialization/twowriter-kill9.test.ts covers the same scenario and passes in the full run.
  ✓ Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence: (a) Authz before enqueue: namespaceWriter.ts enqueue() calls checkAuthorization(provisional,'enqueue') at line 352 BEFORE the queue push at line 384; denial returns FORBIDDEN and never pushes (audit.test.ts 'writes enqueue denials without entering the data queue' asserts pendingRows===0). Re-checked at flush under the lock (line 741, phase 'flush'). HTTP maps FORBIDDEN->403 (src/http/routes/memories.ts:91-92); http-serialization.test.ts 'maps role denial to 403 without writing data and persists a denial audit' asserts 403 + zero data lines + denial audit row. (b) WAL before ack: flushOnce resolves pending promises only AFTER appendFsyncBatch/appendDirectBatch complete (lines 795-800); appendFsyncBatch does writeSync then fdatasyncSync per file (183-240); appendJsonlDurable (src/storage/durability.ts:307-338) does openSync/writeSync/barrierFile(fdatasyncSync)/closeSync + directory fsync before returning. Independent test I wrote and ran (src/__eval__/wal.test.ts, since removed): monkey-patched fs.fdatasyncSync to count barriers, enqueued one write into an fsync-mode namespace, asserted ack resolved with barriers>=1 and record on disk -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. (c) Audit evidence: acceptedAudit() emits {ts,ns,table,op,principal,outcome:'accepted',seq} to <ns>/_audit/current.jsonl (lines 776-782); deniedAudit() emits outcome:'denied'+reason. Independent test I wrote and ran (src/__eval__/audit.test.ts, since removed) asserted accepted rows carry principal alice/bob with seq 1/2 and a denied write yields outcome 'denied'/reason 'role'/principal 'mallory' with zero data rows -> 'Test Files 1 passed (1) / Tests 1 passed (1)'. Audit rows use the buffered path after the data append (documented bounded crash window, lines 790-794) but are staged into git by `git add -A` in immediateCommit (src/git/autocommit.ts:95), so accepted writes retain durable audit evidence in namespace history. Full suite 110 files / 909 tests passed, exit 0; `npx tsc --noEmit` exit 0; LSP diagnostics 0.
All three SUPA-2 criteria verified: per-namespace single-writer fan-in with cross-process locking/fencing/dead-pid recovery, WAL-before-ack in durable modes, pre-enqueue authorization and retained audit evidence — confirmed by the repo's 909-test suite plus four independent tests I wrote and ran.

Overall: PASS ✓
