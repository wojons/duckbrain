{
  "task_id": "DB-SUPA-2",
  "task_title": "SUPA serialization layer",
  "task_criteria": [
    "Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes",
    "Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery",
    "Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence"
  ],
  "passed": false,
  "items": [],
  "stages": {
    "tier1": {
      "id": "tier1",
      "passed": false,
      "any_failed": true,
      "summary": "  \u2713 secrets: \u001b[90m8:27PM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~7216966 bytes (7.22 MB) in 2.14s\u001b[0m\n\u001b[90m8:27PM\u001b[0m \u001b[32m\n  \u2717 tests: \n RUN  v4.1.10 /home/kara/duckbrain\n\n \u276f src/http/routes/memories-dogfood010.test.ts (3 tests | 1 fai",
      "steps": [
        {
          "id": "secrets",
          "type": "script",
          "passed": true,
          "output": "\u001b[90m8:27PM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~7216966 bytes (7.22 MB) in 2.14s\u001b[0m\n\u001b[90m8:27PM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mno leaks found\u001b[0m\n",
          "error": "",
          "data": {
            "exit_code": 0
          }
        },
        {
          "id": "tests",
          "type": "script",
          "passed": false,
          "output": "\n RUN  v4.1.10 /home/kara/duckbrain\n\n \u276f src/http/routes/memories-dogfood010.test.ts (3 tests | 1 failed) 10780ms\n     \u00d7 ?q= over a duplicate-key namespace returns 200 or a clean error and the server stays alive 5031ms\n\n Test Files  1 failed | 109 passed (110)\n      Tests  1 failed | 908 passed (909)\n   Start at  20:27:41\n   Duration  26.60s (transform 11.96s, setup 4.91s, import 91.90s, tests 254.16s, environment 30ms)\n\nstderr | src/http/routes/memories-dogfood010.test.ts > DOGFOOD-010: ?q= surv",
          "error": "",
          "data": {
            "exit_code": 1
          }
        }
      ]
    },
    "tier2": {
      "id": "tier2",
      "passed": true,
      "any_failed": false,
      "summary": "COMPLETE\n  \u2713 Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes: src/serialization/namespaceWriter.ts:686-688 flushOnce() splices the queue under enqueueMutex and sorts by request.seq; appends are whole lines (appendBufferedBatch:251, appendFsyncBatch:196-215, appendDirectBatch:261). Acks resolve only after the append (and, in fsync mode, after fdatasyncSync + fsyncDirectory) at namespaceWriter.ts:790-800. One writer per namespace via the writers Map keyed `${root}\\u0000${ns}` (getNamespaceWriter:836-847). Evidence: `npx vitest run src/serialization/` => 5 files, 24 tests PASSED, including '32 writers across 4 namespaces leave parseable files and no lost acknowledgements' (acknowledged==32, rows==32, every line JSON.parse-able) and 'acked fsync rows survive writer replacement and disk re-read' (8 acked, 8 rows, 8 unique ids). My own adversarial test: 200 concurrent HTTP POSTs across 6 namespaces => 200x 201 acks, 200 rows on disk, 200 unique keys, every line parsed, every file ends with newline. Full suite `npx vitest run` => Test Files 110 passed (110); Tests 909 passed (909).\n  \u2713 Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery: Order: seq assigned at enqueue (namespaceWriter.ts:381 `provisional.seq = ++this.nextSeq`) and flush sorts by seq (:686-688); test 'assigns monotonic seq and flushes data in seq order' asserts results [{seq:1},{seq:2},{seq:3}] and file order [0,1,2]. Lock: src/serialization/lock.ts acquireNamespaceWriteLock uses atomic `fs.openSync(lockPath,\"wx\",0o600)`; shouldBreak() recovers dead holders via isPidAlive (src/utils/pidfile.ts:33-40, signal-0 probe) or age > LOCK_STALE_MS (10min); tokenStillCurrent() re-reads the nonce to fence preempted owners; releaseNamespaceWriteLock only unlinks its own token. Tests: lock.test.ts 'exactly one winner among 16 concurrent wx contenders', 'breaks a dead-pid lock immediately', 'does not break a young lock held by a live pid'. Cross-process: src/serialization/twowriter-kill9.test.ts spawns a REAL child process (tsx) that acks a write then holds the lock; contender gets SERIALIZER_LOCKED; after SIGKILL the contender recovers via dead-pid break and readKeys == ['/child/acked','/parent/acked'] \u2014 PASSED (412ms). Fencing test 'fences a writer whose token is replaced before its first append' => SERIALIZER_FENCED with 0 rows written.\n  \u2713 Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence: Auth before enqueue: namespaceWriter.ts:352-372 calls checkAuthorization(provisional,\"enqueue\") BEFORE queue.push; denial returns FORBIDDEN and enqueues a deniedAudit row, leaving pendingRows 0. My adversarial test confirmed the hook fires with phases == [\"enqueue\"] only, pendingRows === 0, and zero data bytes on disk. WAL before ack: docs/specs/SUPA-2-serialization.md:62 'The JSONL append IS the WAL'; fsync/direct modes run the SUPA-1 barrier (fdatasyncSync per file + fsyncDirectory) inside appendFsyncBatch (namespaceWriter.ts:196-215) BEFORE the waiter promises resolve (:790-800). Audit evidence: acceptedAudit() row is appended to <ns>/_audit/current.jsonl in the SAME batch under the SAME lock, before resolve (:773-778); readAuditRows (src/serialization/audit.ts:41-52) reads it back. Tests: audit.test.ts (accepted row per acked write with principal/table/op/seq; enqueue denial audited; audit exempt from data queue pressure), http-serialization.test.ts 'maps role denial to 403 without writing data and persists a denial audit', serialization-wiring.test.ts (remember+forget both audited 'accepted'; asserts no direct append bypass in remember.ts/queries.ts). My adversarial tests: accepted write => {seq:1,ok:true} + accepted audit row + exactly 1 data row; revocation between enqueue and flush => FORBIDDEN, no data file, denial audited. `_audit` is never scanned by recall: collectJsonlFiles (src/duckdb/queries.ts:143-155) reads only manifest partition paths (domain-scoped), never the namespace root. `npx tsc --noEmit` => EXIT=0; read_lsp_diagnostics => 0 diagnostics.\nAll three SUPA-2 criteria are met: the per-namespace single-writer serializer with cross-process locking, dead-holder recovery, fencing, pre-enqueue authorization, WAL-first durable modes, and per-write audit rows is implemented and verified by the project's own tests plus independent adversarial tests (200 concurrent writes across 6 namespaces: zero torn lines, zero lost acks), with the full suite green at 909/909 and a clean typecheck.",
      "steps": [
        {
          "id": "tier2",
          "type": "ai_eval",
          "passed": true,
          "output": "COMPLETE\n  \u2713 Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes: src/serialization/namespaceWriter.ts:686-688 flushOnce() splices the queue under enqueueMutex and sorts by request.seq; appends are whole lines (appendBufferedBatch:251, appendFsyncBatch:196-215, appendDirectBatch:261). Acks resolve only after the append (and, in fsync mode, after fdatasyncSync + fsyncDirectory) at namespaceWriter.ts:790-800. One writer per ",
          "error": "",
          "data": {
            "verdict": "COMPLETE",
            "items": [
              {
                "criterion": "Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes",
                "status": "PASS",
                "detail": "src/serialization/namespaceWriter.ts:686-688 flushOnce() splices the queue under enqueueMutex and sorts by request.seq; appends are whole lines (appendBufferedBatch:251, appendFsyncBatch:196-215, appendDirectBatch:261). Acks resolve only after the append (and, in fsync mode, after fdatasyncSync + fsyncDirectory) at namespaceWriter.ts:790-800. One writer per namespace via the writers Map keyed `${root}\\u0000${ns}` (getNamespaceWriter:836-847). Evidence: `npx vitest run src/serialization/` => 5 files, 24 tests PASSED, including '32 writers across 4 namespaces leave parseable files and no lost acknowledgements' (acknowledged==32, rows==32, every line JSON.parse-able) and 'acked fsync rows survive writer replacement and disk re-read' (8 acked, 8 rows, 8 unique ids). My own adversarial test: 200 concurrent HTTP POSTs across 6 namespaces => 200x 201 acks, 200 rows on disk, 200 unique keys, every line parsed, every file ends with newline. Full suite `npx vitest run` => Test Files 110 passed (110); Tests 909 passed (909)."
              },
              {
                "criterion": "Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery",
                "status": "PASS",
                "detail": "Order: seq assigned at enqueue (namespaceWriter.ts:381 `provisional.seq = ++this.nextSeq`) and flush sorts by seq (:686-688); test 'assigns monotonic seq and flushes data in seq order' asserts results [{seq:1},{seq:2},{seq:3}] and file order [0,1,2]. Lock: src/serialization/lock.ts acquireNamespaceWriteLock uses atomic `fs.openSync(lockPath,\"wx\",0o600)`; shouldBreak() recovers dead holders via isPidAlive (src/utils/pidfile.ts:33-40, signal-0 probe) or age > LOCK_STALE_MS (10min); tokenStillCurrent() re-reads the nonce to fence preempted owners; releaseNamespaceWriteLock only unlinks its own token. Tests: lock.test.ts 'exactly one winner among 16 concurrent wx contenders', 'breaks a dead-pid lock immediately', 'does not break a young lock held by a live pid'. Cross-process: src/serialization/twowriter-kill9.test.ts spawns a REAL child process (tsx) that acks a write then holds the lock; contender gets SERIALIZER_LOCKED; after SIGKILL the contender recovers via dead-pid break and readKeys == ['/child/acked','/parent/acked'] \u2014 PASSED (412ms). Fencing test 'fences a writer whose token is replaced before its first append' => SERIALIZER_FENCED with 0 rows written."
              },
              {
                "criterion": "Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence",
                "status": "PASS",
                "detail": "Auth before enqueue: namespaceWriter.ts:352-372 calls checkAuthorization(provisional,\"enqueue\") BEFORE queue.push; denial returns FORBIDDEN and enqueues a deniedAudit row, leaving pendingRows 0. My adversarial test confirmed the hook fires with phases == [\"enqueue\"] only, pendingRows === 0, and zero data bytes on disk. WAL before ack: docs/specs/SUPA-2-serialization.md:62 'The JSONL append IS the WAL'; fsync/direct modes run the SUPA-1 barrier (fdatasyncSync per file + fsyncDirectory) inside appendFsyncBatch (namespaceWriter.ts:196-215) BEFORE the waiter promises resolve (:790-800). Audit evidence: acceptedAudit() row is appended to <ns>/_audit/current.jsonl in the SAME batch under the SAME lock, before resolve (:773-778); readAuditRows (src/serialization/audit.ts:41-52) reads it back. Tests: audit.test.ts (accepted row per acked write with principal/table/op/seq; enqueue denial audited; audit exempt from data queue pressure), http-serialization.test.ts 'maps role denial to 403 without writing data and persists a denial audit', serialization-wiring.test.ts (remember+forget both audited 'accepted'; asserts no direct append bypass in remember.ts/queries.ts). My adversarial tests: accepted write => {seq:1,ok:true} + accepted audit row + exactly 1 data row; revocation between enqueue and flush => FORBIDDEN, no data file, denial audited. `_audit` is never scanned by recall: collectJsonlFiles (src/duckdb/queries.ts:143-155) reads only manifest partition paths (domain-scoped), never the namespace root. `npx tsc --noEmit` => EXIT=0; read_lsp_diagnostics => 0 diagnostics."
              }
            ],
            "summary": "All three SUPA-2 criteria are met: the per-namespace single-writer serializer with cross-process locking, dead-holder recovery, fencing, pre-enqueue authorization, WAL-first durable modes, and per-write audit rows is implemented and verified by the project's own tests plus independent adversarial tests (200 concurrent writes across 6 namespaces: zero torn lines, zero lost acks), with the full suite green at 909/909 and a clean typecheck."
          }
        }
      ]
    }
  },
  "summary": "Judge Result: DB-SUPA-2\n\nStage tier1: FAIL\n    \u2713 secrets: \u001b[90m8:27PM\u001b[0m \u001b[32mINF\u001b[0m \u001b[1mscanned ~7216966 bytes (7.22 MB) in 2.14s\u001b[0m\n\u001b[90m8:27PM\u001b[0m \u001b[32m\n  \u2717 tests: \n RUN  v4.1.10 /home/kara/duckbrain\n\n \u276f src/http/routes/memories-dogfood010.test.ts (3 tests | 1 fai\n\nStage tier2: PASS\n  COMPLETE\n  \u2713 Concurrent API writers across multiple namespaces produce zero torn/interleaved JSONL lines and zero lost acknowledged writes: src/serialization/namespaceWriter.ts:686-688 flushOnce() splices the queue under enqueueMutex and sorts by request.seq; appends are whole lines (appendBufferedBatch:251, appendFsyncBatch:196-215, appendDirectBatch:261). Acks resolve only after the append (and, in fsync mode, after fdatasyncSync + fsyncDirectory) at namespaceWriter.ts:790-800. One writer per namespace via the writers Map keyed `${root}\\u0000${ns}` (getNamespaceWriter:836-847). Evidence: `npx vitest run src/serialization/` => 5 files, 24 tests PASSED, including '32 writers across 4 namespaces leave parseable files and no lost acknowledgements' (acknowledged==32, rows==32, every line JSON.parse-able) and 'acked fsync rows survive writer replacement and disk re-read' (8 acked, 8 rows, 8 unique ids). My own adversarial test: 200 concurrent HTTP POSTs across 6 namespaces => 200x 201 acks, 200 rows on disk, 200 unique keys, every line parsed, every file ends with newline. Full suite `npx vitest run` => Test Files 110 passed (110); Tests 909 passed (909).\n  \u2713 Writes are deterministically ordered per namespace through one writer with cross-process locking and dead-holder recovery: Order: seq assigned at enqueue (namespaceWriter.ts:381 `provisional.seq = ++this.nextSeq`) and flush sorts by seq (:686-688); test 'assigns monotonic seq and flushes data in seq order' asserts results [{seq:1},{seq:2},{seq:3}] and file order [0,1,2]. Lock: src/serialization/lock.ts acquireNamespaceWriteLock uses atomic `fs.openSync(lockPath,\"wx\",0o600)`; shouldBreak() recovers dead holders via isPidAlive (src/utils/pidfile.ts:33-40, signal-0 probe) or age > LOCK_STALE_MS (10min); tokenStillCurrent() re-reads the nonce to fence preempted owners; releaseNamespaceWriteLock only unlinks its own token. Tests: lock.test.ts 'exactly one winner among 16 concurrent wx contenders', 'breaks a dead-pid lock immediately', 'does not break a young lock held by a live pid'. Cross-process: src/serialization/twowriter-kill9.test.ts spawns a REAL child process (tsx) that acks a write then holds the lock; contender gets SERIALIZER_LOCKED; after SIGKILL the contender recovers via dead-pid break and readKeys == ['/child/acked','/parent/acked'] \u2014 PASSED (412ms). Fencing test 'fences a writer whose token is replaced before its first append' => SERIALIZER_FENCED with 0 rows written.\n  \u2713 Role authorization happens before enqueue, durable modes write WAL before acknowledgement, and accepted writes retain audit evidence: Auth before enqueue: namespaceWriter.ts:352-372 calls checkAuthorization(provisional,\"enqueue\") BEFORE queue.push; denial returns FORBIDDEN and enqueues a deniedAudit row, leaving pendingRows 0. My adversarial test confirmed the hook fires with phases == [\"enqueue\"] only, pendingRows === 0, and zero data bytes on disk. WAL before ack: docs/specs/SUPA-2-serialization.md:62 'The JSONL append IS the WAL'; fsync/direct modes run the SUPA-1 barrier (fdatasyncSync per file + fsyncDirectory) inside appendFsyncBatch (namespaceWriter.ts:196-215) BEFORE the waiter promises resolve (:790-800). Audit evidence: acceptedAudit() row is appended to <ns>/_audit/current.jsonl in the SAME batch under the SAME lock, before resolve (:773-778); readAuditRows (src/serialization/audit.ts:41-52) reads it back. Tests: audit.test.ts (accepted row per acked write with principal/table/op/seq; enqueue denial audited; audit exempt from data queue pressure), http-serialization.test.ts 'maps role denial to 403 without writing data and persists a denial audit', serialization-wiring.test.ts (remember+forget both audited 'accepted'; asserts no direct append bypass in remember.ts/queries.ts). My adversarial tests: accepted write => {seq:1,ok:true} + accepted audit row + exactly 1 data row; revocation between enqueue and flush => FORBIDDEN, no data file, denial audited. `_audit` is never scanned by recall: collectJsonlFiles (src/duckdb/queries.ts:143-155) reads only manifest partition paths (domain-scoped), never the namespace root. `npx tsc --noEmit` => EXIT=0; read_lsp_diagnostics => 0 diagnostics.\nAll three SUPA-2 criteria are met: the per-namespace single-writer serializer with cross-process locking, dead-holder recovery, fencing, pre-enqueue authorization, WAL-first durable modes, and per-write audit rows is implemented and verified by the project's own tests plus independent adversarial tests (200 concurrent writes across 6 namespaces: zero torn lines, zero lost acks), with the full suite green at 909/909 and a clean typecheck.\n\nOverall: FAIL \u2717",
  "evaluated_at": "2026-09-11T01:36:22.996634"
}