---
phase: 04.1-ui-data-integration-polish
plan: 01
completed: 2026-04-03
duration: 15m
subsystem: UI
key-decisions:
  - Used useQueries instead of sequential API calls for better performance
  - Changed tombstoneRatio from string to number type for easier formatting
  - Implemented loading/error states with "Loading..." and "Error" fallback text
  - Fixed existing import path bug in offline-banner.tsx (deviation)
tech-stack:
  added:
    - TanStack Query useQueries for parallel fetching
  patterns:
    - Parallel API requests via useQueries
    - Co-located loading state handling in components
    - Number-based ratios formatted in presentation layer
key-files:
  created:
    - packages/ui/src/hooks/use-vitals.ts (refactored from existing)
  modified:
    - packages/ui/src/routes/Timeline.tsx
    - packages/ui/src/routes/Tree.tsx
    - packages/ui/src/components/ui/offline-banner.tsx
requires: []
provides:
  - Real-time vitals display in Timeline view
  - Real-time vitals display in Tree view
affects:
  - Timeline.tsx vitals section
  - Tree.tsx vitals section
---

# Phase 04.1 Plan 01: Vitals Data Integration Summary

## Overview

Successfully replaced hardcoded "—" values in Vitals widgets with real data from the API. Both Timeline and Tree pages now display live system metrics.

## What Was Built

### useVitals Hook (`packages/ui/src/hooks/use-vitals.ts`)

**Before:** Single sequential API calls, string-based tombstoneRatio, no loading state

**After:** 
- Parallel data fetching using `useQueries` for efficiency
- Number-based metrics (tombstoneRatio as number, formatted with % in UI)
- Proper loading and error state handling
- Derived from existing memories and keys APIs

**Metrics Calculated:**
1. **Active Memories**: Total count from `/api/memories` endpoint
2. **Git Queue**: Placeholder (0) - no git queue API exposed yet
3. **Tombstone Ratio**: Calculated from 100-item sample (`tombstones / total * 100`)
4. **Key Count**: Total keys from `/api/keys` endpoint

### Timeline.tsx Updates

**Changes:**
- Destructured `isLoading` and `error` from `useVitals`
- Added conditional value display:
  - Loading: "Loading..."
  - Error: "Error"
  - Success: Actual numeric values with proper formatting
- Tombstone ratio now formats as "XX.X%"

### Tree.tsx Updates

**Changes:**
- Identical vitals implementation to Timeline.tsx
- Ensures consistency across both view modes
- Same loading/error handling

## Verification Results

- ✅ useVitals hook exists and is properly typed
- ✅ Timeline.tsx displays real numbers instead of "—"
- ✅ Tree.tsx displays real numbers instead of "—"
- ✅ Loading states show "Loading..." text
- ✅ Error states show "Error" text
- ✅ No TypeScript errors (build successful)
- ✅ Vitals update when namespace changes (via queryKey dependency)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed import path in offline-banner.tsx**
- **Found during:** Build verification
- **Issue:** Import path `../hooks/use-online-status` was incorrect (should be `../../hooks/`)
- **Fix:** Updated to correct relative path
- **Files modified:** `packages/ui/src/components/ui/offline-banner.tsx`
- **Commit:** 3134790

**2. [Rule 2 - Auto-improvement] Enhanced hook implementation**
- **Found during:** Task 1 execution
- **Issue:** Original hook used sequential API calls instead of parallel
- **Fix:** Refactored to use `useQueries` for parallel fetching
- **Result:** Better performance, cleaner code
- **Commit:** 1ab6eb8

**3. [Rule 2 - Auto-improvement] Changed tombstoneRatio type**
- **Found during:** Implementation
- **Issue:** Hook returned string with "%" already appended, causing double formatting
- **Fix:** Changed `tombstoneRatio: string` to `tombstoneRatio: number` in VitalsData interface
- **Result:** Consistent numeric types, formatted at presentation layer
- **Commit:** 1ab6eb8

## Commits

| Commit | Message |
|--------|---------|
| 1ab6eb8 | feat(04.1-01): update useVitals hook to use useQueries for parallel fetching |
| 46b9c1a | feat(04.1-01): update Timeline and Tree to show real vitals data with loading states |
| 3134790 | fix(04.1-01): correct import path in offline-banner component |

## Known Limitations

1. **Git Queue**: Currently returns 0 (placeholder) - no git queue API endpoint exists yet
2. **Query Rate**: Actually shows Key Count - no query tracking implemented server-side
3. **Tombstone Ratio**: Calculated from 100-item sample - may not be statistically significant for large datasets

These limitations are tracked for future enhancement when dedicated endpoints become available.

## Self-Check: PASSED

- ✅ use-vitals.ts exists and compiles
- ✅ Timeline.tsx displays real data
- ✅ Tree.tsx displays real data
- ✅ All commits recorded
- ✅ Build successful (`npm run build` passes)
