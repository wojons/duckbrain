---
status: testing
phase: 04.1-ui-data-integration-polish
source: [04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md]
started: 2026-04-02T19:45:00Z
updated: 2026-04-02T19:45:00Z
---

## Current Test

number: 1
name: Vitals Widgets Display Real Data
expected: |
  Timeline and Tree pages show actual metrics in Vitals widgets:
  - Active Memories shows real count from API (not "—")
  - Git Queue shows actual queue status
  - Tombstone Ratio calculated from memory data
  - Query Rate displays actual value
awaiting: user response

## Tests

### 1. Vitals Widgets Display Real Data
expected: Vitals widgets show actual API data instead of "—" placeholder
criteria: |
  - Active Memories displays real count
  - Git Queue shows actual status
  - Tombstone Ratio calculated from data
  - Query Rate shows real value
result: [pending]

### 2. Tree View Loading States
expected: Tree view shows glassmorphism skeletons while loading
criteria: |
  - SkeletonTree component displays on initial load
  - Hierarchical folder structure visible in skeleton
  - Smooth transition to actual data
result: [pending]

### 3. Tree Folder Expand/Collapse
expected: Clicking folder chevron expands/collapses tree nodes
criteria: |
  - Chevron click toggles folder open/close
  - Folder contents animate smoothly
  - State persists during navigation
result: pass

### 4. Memory Click Opens Inspector
expected: Clicking memory row opens inspector panel with details
criteria: |
  - Row click triggers inspector
  - Inspector slides in from right
  - Memory details populate in inspector
  - Main content resizes to accommodate panel
result: issue
reported: "Tree click passes key path (/test/batch-1) but API expects UUID. Inspector tries to fetch /memories//test/batch-1 which 404s"
severity: major

### 5. Error Boundaries with Retry
expected: API errors show glassmorphism error card with retry button
criteria: |
  - ErrorCard component displays on failure
  - Red border styling matches theme
  - Retry button calls refetch()
  - Graceful fallback rendering
result: [pending]

### 6. Table Loading Skeletons
expected: Memory table shows skeleton rows while loading
criteria: |
  - SkeletonTable displays with correct column count
  - Smooth pulse animation
  - Transitions to actual data
result: [pending]

### 7. Selected Memory Highlighting
expected: Selected memory row highlighted in tree and table
criteria: |
  - Visual highlight (border-azure/30)
  - Background change (bg-white/10)
  - Highlight persists during scroll
result: [pending]

### 8. No Hardcoded Mock Data
expected: All UI data comes from API, no "—" or placeholder values remain
criteria: |
  - Audit all UI components
  - Verify dynamic data sources
  - Confirm API integration
result: [pending]

### 9. SSE Real-time Updates
expected: UI receives real-time updates via SSE connection
criteria: |
  - Connection established to /api/events
  - Memory updates reflect immediately
  - Reconnection after disconnect
result: [pending]

## Summary

total: 9
passed: 1
issues: 1
pending: 7
skipped: 0
blocked: 0

## Gaps

- truth: "Memory click opens inspector with memory details"
  status: failed
  reason: "Tree click passes key path (/test/batch-1) but API expects UUID. Inspector tries to fetch /memories//test/batch-1 which 404s"
  severity: major
  test: 4
  root_cause: "KeyNode.id is the key path, not memory UUID. Inspector uses useMemory() which calls /memories/:id endpoint expecting UUID."
  artifacts:
    - path: "packages/ui/src/components/memory-tree.tsx"
      issue: "Tree passes node.id (key path) to setSelectedMemory()"
    - path: "packages/ui/src/components/layout/inspector.tsx"
      issue: "Inspector calls useMemory(selectedMemory) which expects UUID"
    - path: "packages/ui/src/hooks/use-memories.ts"
      issue: "useMemory hook only accepts UUIDs, not key paths"
  missing:
    - "Add getByKey endpoint to fetch memories by key path"
    - "Update inspector to detect key paths vs UUIDs"
    - "Create useMemoryByKey hook for key-based lookups"
  debug_session: ".planning/debug/tree-inspector-key-path-issue.md"
