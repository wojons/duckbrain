---
phase: 04-web-ui
plan: 02
type: execute
wave: 2
depends_on: [04-web-ui-01]
files_modified:
  - src/cli/http.ts
  - src/http/routes/memories.ts
  - src/http/routes/keys.ts
  - src/http/routes/namespaces.ts
  - src/http/routes/events.ts
  - src/http/middleware/errorHandler.ts
  - src/http/types/api.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - Express API endpoints return JSON data from MCP tools
    - GET /api/memories returns memory list with pagination
    - GET /api/keys returns hierarchical key tree
    - GET /api/namespaces returns namespace list
    - POST /api/events establishes SSE connection
    - CORS configured for UI development on localhost:5173
  artifacts:
    - path: src/http/routes/memories.ts
      provides: "Memory CRUD API endpoints"
      exports: ["GET /api/memories", "POST /api/memories", "PUT /api/memories/:id", "DELETE /api/memories/:id"]
    - path: src/http/routes/keys.ts
      provides: "Key tree API"
      exports: ["GET /api/keys"]
    - path: src/http/routes/namespaces.ts
      provides: "Namespace management API"
      exports: ["GET /api/namespaces", "POST /api/namespaces", "POST /api/namespaces/switch"]
    - path: src/http/routes/events.ts
      provides: "Server-sent events endpoint"
      exports: ["GET /api/events/:namespace"]
    - path: src/http/types/api.ts
      provides: "API request/response type definitions"
      exports: ["MemoryResponse", "KeyNode", "NamespaceResponse"]
  key_links:
    - from: "src/cli/http.ts"
      to: "src/http/routes/*.ts"
      via: "app.use('/api', routes)"
    - from: "src/http/routes/memories.ts"
      to: "src/mcp/tools/recall.ts"
      via: "import { recallTool }"
    - from: "src/http/routes/memories.ts"
      to: "src/mcp/tools/remember.ts"
      via: "import { rememberTool }"
    - from: "src/http/routes/keys.ts"
      to: "src/mcp/tools/list_keys.ts"
      via: "import { listKeysTool }"
---

<objective>
Implement Express API routes that wrap MCP tool functions, providing HTTP endpoints for the Web UI to query and mutate memory data. All data flows through existing MCP tools (recallTool, rememberTool, listKeysTool, forgetTool) per the centralized architecture pattern.

Purpose: Enable the React UI to access memory data via HTTP API instead of DuckDB-WASM (revised per RESEARCH.md).
Output: Working REST API endpoints with SSE support for real-time updates.
</objective>

<execution_context>
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md
@src/cli/http.ts
@src/mcp/tools/recall.ts
@src/mcp/tools/remember.ts
@src/mcp/tools/list_keys.ts
@src/mcp/tools/forget.ts

From RESEARCH.md — Centralized Architecture Pattern:
"All interfaces use same MCP tool functions; HTTP API is thin wrapper"

API Design Pattern:
- GET /api/memories → recallTool()
- POST /api/memories → rememberTool()
- PUT /api/memories/:id → edit (forget + remember)
- DELETE /api/memories/:id → forgetTool()
- GET /api/keys → listKeysTool()
- GET /api/namespaces → listNamespacesTool()
- GET /api/events/:namespace → SSE endpoint

Per D-17: API-First Data Access — all queries via HTTP API
Per D-18: HTTP API for writes (remember, forget)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTTP API types and error handling middleware</name>
  <files>src/http/types/api.ts, src/http/middleware/errorHandler.ts</files>
  <action>
Create type definitions and error handling for the HTTP API layer:

1. src/http/types/api.ts:
   - Define TypeScript interfaces for API request/response types:
     - MemoryResponse: id, key, domain, content, attributes, timestamp, author, isTombstone
     - KeyNode: id, name, path, type ('folder' | 'memory'), children?, isExpanded?
     - NamespaceResponse: name, path, memoryCount, lastModified
     - CreateMemoryRequest: key, domain, content, attributes?
     - UpdateMemoryRequest: content, attributes?
     - QueryParams: prefix?, limit?, offset?, domain?, author?

2. src/http/middleware/errorHandler.ts:
   - Export errorHandler middleware function
   - Catch errors and return JSON: { error: string, code?: string }
   - Handle specific error types:
     - Validation errors (Zod): 400 with field details
     - Not found errors: 404
     - Server errors: 500
   - Log errors to console for debugging

3. Create src/http/routes/index.ts barrel file:
   - Export all route modules from a central location
  </action>
  <verify>
    <automated>test -f src/http/types/api.ts && test -f src/http/middleware/errorHandler.ts && grep -q "MemoryResponse" src/http/types/api.ts && grep -q "export function errorHandler" src/http/middleware/errorHandler.ts</automated>
  </verify>
  <done>API types and error handling middleware created</done>
</task>

<task type="auto">
  <name>Task 2: Implement memory and keys API routes</name>
  <files>src/http/routes/memories.ts, src/http/routes/keys.ts</files>
  <action>
Create API routes that wrap MCP tool functions per the centralized architecture:

1. src/http/routes/memories.ts:
   - Import { Router } from 'express'
   - Import MCP tools: recallTool, rememberTool, forgetTool from '../../mcp/tools/'
   - Create router and define endpoints:
   
   GET /api/memories:
   - Query params: prefix?, limit?, offset?, domain?, author?
   - Call recallTool with filters
   - Return { memories: MemoryResponse[], total: number }

   GET /api/memories/:id:
   - Path param: id
   - Call recallTool with exact key lookup
   - Return single MemoryResponse or 404

   POST /api/memories:
   - Body: key, domain, content, attributes?
   - Call rememberTool
   - Return 201 with created memory

   PUT /api/memories/:id:
   - Path param: id
   - Body: content, attributes?
   - Implementation: 
     1. Fetch existing memory via recallTool
     2. Call forgetTool to create tombstone
     3. Call rememberTool with new content (new version)
     4. Return updated memory with version history

   DELETE /api/memories/:id:
   - Path param: id
   - Call forgetTool
   - Return 204 No Content

2. src/http/routes/keys.ts:
   - Import { Router } from 'express'
   - Import listKeysTool from '../../mcp/tools/list_keys'
   
   GET /api/keys:
   - Query params: prefix?, limit?, depth?
   - Call listKeysTool with prefix filter
   - Transform response to KeyNode tree structure
   - Return { keys: KeyNode[], total: number }

3. Test endpoints:
   - Start HTTP server with npm run dev:http
   - Test GET /api/keys with curl
   - Test GET /api/memories with curl
  </action>
  <verify>
    <automated>test -f src/http/routes/memories.ts && test -f src/http/routes/keys.ts && grep -q "recallTool" src/http/routes/memories.ts && grep -q "rememberTool" src/http/routes/memories.ts && grep -q "listKeysTool" src/http/routes/keys.ts && grep -q "Router()" src/http/routes/memories.ts</automated>
  </verify>
  <done>Memory and keys API routes implemented, calling MCP tools</done>
</task>

<task type="auto">
  <name>Task 3: Implement namespace and SSE events routes</name>
  <files>src/http/routes/namespaces.ts, src/http/routes/events.ts, src/cli/http.ts</files>
  <action>
Create namespace and real-time events endpoints:

1. src/http/routes/namespaces.ts:
   - Import MCP tools: listNamespacesTool, createNamespaceTool, switchNamespaceTool
   
   GET /api/namespaces:
   - Call listNamespacesTool
   - Return { namespaces: NamespaceResponse[] }

   POST /api/namespaces:
   - Body: name
   - Call createNamespaceTool
   - Return 201 with created namespace

   POST /api/namespaces/switch:
   - Body: name
   - Call switchNamespaceTool
   - Return 200

2. src/http/routes/events.ts (Server-Sent Events):
   - Import { Router } from 'express'
   
   GET /api/events/:namespace:
   - Set headers: Content-Type: text/event-stream, Cache-Control: no-cache, Connection: keep-alive
   - Create EventSource-style response
   - Keep connection open
   - Send events in format: data: {json}\n\n
   - Handle client disconnect (req.on('close'))
   
   Note: For Phase 4, implement basic SSE structure. Full event publishing integration with memory changes can be enhanced later.

3. Update src/cli/http.ts:
   - Import new route modules
   - Register routes:
     - app.use('/api/memories', memoryRoutes)
     - app.use('/api/keys', keyRoutes)
     - app.use('/api/namespaces', namespaceRoutes)
     - app.use('/api/events', eventsRoutes)
   - Add CORS middleware for localhost:5173 (UI dev server)
   - Move existing stub endpoints to proper route modules or remove

4. Test the complete API:
   - Start server: npm run dev:http
   - Test: curl http://localhost:3000/api/namespaces
   - Test: curl http://localhost:3000/api/keys
   - Verify CORS headers present
  </action>
  <verify>
    <automated>test -f src/http/routes/namespaces.ts && test -f src/http/routes/events.ts && grep -q "text/event-stream" src/http/routes/events.ts && grep -q "app.use('/api/memories'" src/cli/http.ts && grep -q "app.use('/api/keys'" src/cli/http.ts</automated>
  </verify>
  <done>Namespace and SSE routes implemented, HTTP server updated with all API routes</done>
</task>

</tasks>

<verification>
- GET /api/memories returns JSON array
- GET /api/keys returns hierarchical tree
- GET /api/namespaces returns namespace list
- POST /api/memories creates memory (test with curl)
- CORS headers allow localhost:5173
- All routes call MCP tools, not direct storage access
</verification>

<success_criteria>
- All API endpoints return correct JSON responses
- MCP tools are the single source of truth (no direct DB access)
- CORS configured for UI development
- Error handling returns structured JSON errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-02-SUMMARY.md`
</output>