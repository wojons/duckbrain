---
phase: 04-web-ui
plan: 03
type: execute
wave: 3
depends_on: [04-web-ui-01, 04-web-ui-02]
files_modified:
  - packages/ui/src/lib/api-client.ts
  - packages/ui/src/hooks/use-memories.ts
  - packages/ui/src/hooks/use-keys.ts
  - packages/ui/src/hooks/use-namespaces.ts
  - packages/ui/src/hooks/use-sse.ts
  - packages/ui/src/components/layout/sidebar.tsx
  - packages/ui/src/components/layout/inspector.tsx
  - packages/ui/src/components/memory-table.tsx
  - packages/ui/src/components/memory-tree.tsx
  - packages/ui/src/routes/Tree.tsx
  - packages/ui/src/routes/Timeline.tsx
  - packages/ui/src/stores/ui-store.ts
autonomous: true
requirements: [UI-01, UI-04, UI-05]
must_haves:
  truths:
    - Tree view displays hierarchical memory keys with expand/collapse
    - Timeline view shows memories in chronological order with TanStack Table
    - Inspector panel slides out from right showing memory details
    - Real-time updates work via SSE with pause/resume controls
    - Namespace selector switches between namespaces
    - Clicking memory opens inspector with JSON details
  artifacts:
    - path: packages/ui/src/lib/api-client.ts
      provides: "HTTP API client with typed methods"
      exports: ["memoriesApi", "keysApi", "namespacesApi"]
    - path: packages/ui/src/hooks/use-memories.ts
      provides: "TanStack Query hooks for memories"
      exports: ["useMemories", "useCreateMemory", "useUpdateMemory", "useForgetMemory"]
    - path: packages/ui/src/components/memory-tree.tsx
      provides: "Hierarchical tree navigation component"
      exports: ["MemoryTree"]
    - path: packages/ui/src/components/memory-table.tsx
      provides: "TanStack Table for timeline view"
      exports: ["MemoryTable"]
    - path: packages/ui/src/components/layout/inspector.tsx
      provides: "Slide-out memory inspector panel"
      exports: ["InspectorPanel"]
    - path: packages/ui/src/routes/Tree.tsx
      provides: "Tree view page"
      contains: ["MemoryTree", "MemoryTable integration"]
    - path: packages/ui/src/routes/Timeline.tsx
      provides: "Timeline view page"
      contains: ["TanStack Table", "virtual scrolling"]
  key_links:
    - from: "packages/ui/src/hooks/use-memories.ts"
      to: "packages/ui/src/lib/api-client.ts"
      via: "import { memoriesApi }"
    - from: "packages/ui/src/components/memory-table.tsx"
      to: "packages/ui/src/hooks/use-memories.ts"
      via: "const { data } = useMemories()"
    - from: "packages/ui/src/components/layout/inspector.tsx"
      to: "packages/ui/src/hooks/use-memories.ts"
      via: "useMutation for updates"
    - from: "packages/ui/src/hooks/use-sse.ts"
      to: "EventSource('/api/events/...')"
      via: "native EventSource API"
---

<objective>
Implement the Memory Explorer UI with tree navigation, timeline table, inspector panel, and real-time updates. Build React components using TanStack Table, Query, and Virtual for performance with large datasets.

Purpose: Deliver the core Web UI experience for browsing and exploring memories.
Output: Working Tree view, Timeline view, Inspector panel, and real-time update system.
</objective>

<execution_context>
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md

From CONTEXT.md — Layout Structure:
- D-24: Left sidebar (256px) — Archive Navigation with Memory Tree
- D-25: Main stage — Timeline feed or Tree view
- D-26: Right inspector (450px, slide-out) — Memory details

From RESEARCH.md — Patterns:
- TanStack Table with virtualization for 100k+ rows
- SSE with TanStack Query integration
- Tree navigation with lazy loading
- Slide-out inspector panel
- Glassmorphism theme classes

Per D-08: File-explorer-style tree with collapsible folders
Per D-11: Chronological feed with time grouping
Per D-14: Real-time search with debouncing
Per D-28: Timeline table with columns: State, Key Path, Domain, Action, Timestamp
Per D-30: Memory Inspector with JSON viewer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API client and TanStack Query hooks</name>
  <files>packages/ui/src/lib/api-client.ts, packages/ui/src/hooks/use-memories.ts, packages/ui/src/hooks/use-keys.ts, packages/ui/src/hooks/use-namespaces.ts</files>
  <action>
Create typed API client and custom hooks following RESEARCH.md patterns:

1. packages/ui/src/lib/api-client.ts:
   - Create base fetch wrapper with error handling
   - Define API_BASE = '/api'
   - Export typed API modules:
     - memoriesApi: { list, get, create, update, delete, getVersions }
     - keysApi: { list }
     - namespacesApi: { list, create, switch }
   - Handle JSON parsing and HTTP errors
   - Add proper TypeScript return types using types from api.ts

2. packages/ui/src/hooks/use-memories.ts:
   - Import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
   - Export useMemories(params?) — cached list query with 30s staleTime
   - Export useMemory(id) — single memory query
   - Export useCreateMemory() — mutation with invalidateQueries on success
   - Export useUpdateMemory() — mutation for editing
   - Export useForgetMemory() — mutation for deletion
   - All mutations should invalidate ['memories'] and ['keys'] queries

3. packages/ui/src/hooks/use-keys.ts:
   - Export useKeys(prefix?) — query for key tree
   - Use queryKey: ['keys', prefix]

4. packages/ui/src/hooks/use-namespaces.ts:
   - Export useNamespaces() — list query
   - Export useCurrentNamespace() — from Zustand store
   - Export useSwitchNamespace() — mutation

5. Create packages/ui/src/stores/ui-store.ts with Zustand:
   - selectedMemory: string | null
   - inspectorOpen: boolean
   - sidebarCollapsed: boolean
   - currentNamespace: string
   - Actions: setSelectedMemory, toggleInspector, setSidebarCollapsed, setCurrentNamespace
  </action>
  <verify>
    <automated>test -f packages/ui/src/lib/api-client.ts && test -f packages/ui/src/hooks/use-memories.ts && test -f packages/ui/src/stores/ui-store.ts && grep -q "useQuery" packages/ui/src/hooks/use-memories.ts && grep -q "zustand" packages/ui/src/stores/ui-store.ts</automated>
  </verify>
  <done>API client and TanStack Query hooks created with proper caching</done>
</task>

<task type="auto">
  <name>Task 2: Build Memory Tree and layout components</name>
  <files>packages/ui/src/components/memory-tree.tsx, packages/ui/src/components/layout/sidebar.tsx, packages/ui/src/components/layout/inspector.tsx</files>
  <action>
Create the core layout and navigation components per D-24, D-26, D-27:

1. packages/ui/src/components/memory-tree.tsx:
   - Import { useState } from 'react'
   - Import { useKeys } from '../../hooks/use-keys'
   - Import { Folder, FolderOpen, FileJson, ChevronRight, ChevronDown } from 'lucide-react'
   - Define TreeNode interface
   - Create recursive TreeNodeComponent:
     - Show folder or file icon based on type
     - Chevron indicator for expandable folders
     - Indentation based on depth level
     - Click to expand/collapse (load children via API)
     - Click file to select memory (update ui-store)
   - Use glass-panel classes for styling
   - Lazy loading: fetch children only when expanding

2. packages/ui/src/components/layout/sidebar.tsx:
   - Fixed width 256px, full height
   - Glass panel styling with right border
   - Sections:
     - Header: "DuckBrain" logo/branding
     - Namespaces section with dropdown selector
     - Memory Tree section (use MemoryTree component)
     - Bottom: Settings/System Config button
   - Collapsible on mobile (hamburger menu)
   - Thin custom scrollbar styling

3. packages/ui/src/components/layout/inspector.tsx:
   - Fixed width 450px, slides from right
   - Use ui-store for isOpen state
   - Content when memory selected:
     - Header: "Memory Inspector" with close button (X icon)
     - Details grid: Key, Domain, Author, Timestamp
     - JSON viewer with syntax highlighting (dark bg #060913)
     - Footer: Copy JSON button, Forget button
   - Transform animation: translate-x-full when closed
   - Glass panel with left border accent
   - Use azure color for JSON keys, amber for strings

4. packages/ui/src/components/layout/header.tsx:
   - Omnibar search input (centered, rounded-full)
   - Real-time controls (pause/resume/refresh)
   - View toggle (Tree/Timeline)
  </action>
  <verify>
    <automated>test -f packages/ui/src/components/memory-tree.tsx && test -f packages/ui/src/components/layout/sidebar.tsx && test -f packages/ui/src/components/layout/inspector.tsx && grep -q "ChevronRight" packages/ui/src/components/memory-tree.tsx && grep -q "translate-x-full" packages/ui/src/components/layout/inspector.tsx</automated>
  </verify>
  <done>Tree navigation and layout components built with glassmorphism styling</done>
</task>

<task type="auto">
  <name>Task 3: Build Timeline table with SSE and route pages</name>
  <files>packages/ui/src/components/memory-table.tsx, packages/ui/src/hooks/use-sse.ts, packages/ui/src/routes/Tree.tsx, packages/ui/src/routes/Timeline.tsx, packages/ui/src/App.tsx</files>
  <action>
Create the timeline view with TanStack Table and wire up all routes:

1. packages/ui/src/components/memory-table.tsx:
   - Import { useReactTable, getCoreRowModel } from '@tanstack/react-table'
   - Import { useVirtualizer } from '@tanstack/react-virtual'
   - Import { useMemories } from '../hooks/use-memories'
   - Define columns per D-28:
     - State: dot indicator (active=tombstone styling)
     - Key Path: monospace text
     - Domain: badge/pill styling
     - Action: badge showing action type
     - Timestamp: formatted date
   - Implement virtual scrolling for performance (estimateSize: 48px)
   - Row click handler: set selected memory + open inspector
   - Tombstone rows: 40% opacity, strikethrough key
   - Glass panel container with hover states

2. packages/ui/src/hooks/use-sse.ts:
   - Import { useEffect, useRef, useCallback } from 'react'
   - Import { useQueryClient } from '@tanstack/react-query'
   - Export useSSE({ namespace, onMessage?, enabled? }):
     - Create EventSource connection to /api/events/:namespace
     - Handle onmessage: parse JSON, invalidate queries based on event type
     - Handle onerror: exponential backoff reconnection
     - Export pause/resume functions
     - Export isPaused state
     - Clean up on unmount

3. packages/ui/src/routes/Tree.tsx:
   - Layout: Sidebar (left), Main content (flex-1)
   - Main content shows MemoryTree as primary view
   - Optional: Split view with table below tree
   - Include InspectorPanel on right

4. packages/ui/src/routes/Timeline.tsx:
   - Layout: Sidebar (left), Main content (flex-1)
   - Header with Omnibar search
   - Vitals widgets row (Active Memories, Git Queue, Tombstone Ratio)
   - MemoryTable as main content
   - Include InspectorPanel on right

5. Update packages/ui/src/App.tsx:
   - Import new routes
   - Set up Router with all routes:
     - "/" → Timeline (default)
     - "/tree" → Tree
     - "/timeline" → Timeline
   - Wrap with QueryClientProvider and UI store provider

6. Test the complete UI:
   - npm run dev in packages/ui
   - Verify tree navigation works
   - Verify timeline loads memories
   - Verify inspector opens on memory click
   - Check glassmorphism styling throughout
  </action>
  <verify>
    <automated>test -f packages/ui/src/components/memory-table.tsx && test -f packages/ui/src/hooks/use-sse.ts && test -f packages/ui/src/routes/Tree.tsx && test -f packages/ui/src/routes/Timeline.tsx && grep -q "useReactTable" packages/ui/src/components/memory-table.tsx && grep -q "EventSource" packages/ui/src/hooks/use-sse.ts</automated>
  </verify>
  <done>Timeline table, SSE hooks, and route pages complete — full UI functional</done>
</task>

</tasks>

<verification>
- Tree view displays hierarchical keys and expands/collapses
- Timeline shows memories in table with virtual scrolling
- Inspector panel slides out and shows memory details + JSON
- SSE connection established and receives updates
- Namespace selector switches active namespace
- Glassmorphism theme consistent across all components
- UI connects to Express API running on localhost:3000
</verification>

<success_criteria>
- All three views (Tree, Timeline, Inspector) functional
- Real-time updates via SSE working
- TanStack Query caching effective
- Virtual scrolling smooth with large datasets
- API integration complete (no mock data)
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-03-SUMMARY.md`
</output>