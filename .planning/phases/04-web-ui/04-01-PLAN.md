---
phase: 04-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/vite.config.ts
  - packages/ui/tsconfig.json
  - packages/ui/tailwind.config.ts
  - packages/ui/src/main.tsx
  - packages/ui/src/App.tsx
  - packages/ui/src/styles/globals.css
  - packages/ui/index.html
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - Vite dev server starts successfully on port 5173
    - Tailwind CSS classes render correctly with custom colors
    - Glassmorphism theme renders with proper backdrop blur
    - React Router client-side navigation works
  artifacts:
    - path: packages/ui/package.json
      provides: "UI dependencies and scripts"
      contains: ["vite", "react", "tailwindcss", "@tanstack/react-query"]
    - path: packages/ui/vite.config.ts
      provides: "Vite build configuration"
      exports: ["default config with React plugin"]
    - path: packages/ui/tailwind.config.ts
      provides: "Custom theme colors and fonts"
      contains: ["colors: { midnight, azure, amber, pristine, clinical, success }"]
    - path: packages/ui/src/styles/globals.css
      provides: "Glassmorphism utility classes"
      contains: [".glass-panel", "backdrop-blur-glass"]
    - path: packages/ui/src/main.tsx
      provides: "React app entry point"
      contains: ["ReactDOM.createRoot", "QueryClientProvider"]
    - path: packages/ui/src/App.tsx
      provides: "Root component with router"
      contains: ["BrowserRouter", "Routes", "Route"]
  key_links:
    - from: "packages/ui/src/main.tsx"
      to: "packages/ui/src/App.tsx"
      via: "import App from './App'"
    - from: "packages/ui/vite.config.ts"
      to: "tailwind.config.ts"
      via: "tailwindcss plugin"
---

<objective>
Create the UI foundation with Vite, React, TypeScript, and Tailwind CSS configured for the Architectural Cybernetics glassmorphism theme. Set up the build tooling, custom Tailwind theme, and basic React Router structure.

Purpose: Establish the foundation for the DuckBrain Web UI with all build tools and theming configured correctly.
Output: A working Vite+React development environment with custom glassmorphism Tailwind theme.
</objective>

<execution_context>
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/lexykwaii/Code/duckbrain/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

From RESEARCH.md — Tailwind config with custom colors:
```typescript
colors: {
  midnight: '#0B101E',
  glass: {
    DEFAULT: 'rgba(255, 255, 255, 0.03)',
    hover: 'rgba(255, 255, 255, 0.06)',
    border: 'rgba(255, 255, 255, 0.08)',
  },
  azure: '#00D4FF',
  amber: '#FFB020',
  pristine: '#F8FAFC',
  clinical: '#94A3B8',
  success: '#00FF66',
}
```

Per D-04: React with TypeScript for UI framework
Per D-06: Tailwind CSS for styling
Per D-07: Vite for build tooling
Per D-19-D-23: Glassmorphism theme with specific colors
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI package structure and package.json</name>
  <files>packages/ui/package.json, packages/ui/tsconfig.json</files>
  <action>
Create a new UI package at `packages/ui/` with:

1. package.json with these dependencies (per D-04, D-06, D-07, RESEARCH.md):
   - react@19.x, react-dom@19.x
   - react-router@7.x
   - @tanstack/react-query@5.x
   - tailwindcss@4.x, @tailwindcss/postcss, postcss
   - lucide-react
   - clsx, tailwind-merge
   - zustand@5.x
   - react-hook-form@7.x, @hookform/resolvers, zod

2. Dev dependencies:
   - typescript@5.x
   - @types/react, @types/react-dom
   - @vitejs/plugin-react@4.x
   - vite@6.x

3. Scripts:
   - "dev": "vite"
   - "build": "tsc && vite build"
   - "preview": "vite preview"

4. tsconfig.json:
   - "target": "ES2020"
   - "module": "ESNext"
   - "moduleResolution": "bundler"
   - "jsx": "react-jsx"
   - "strict": true
   - "baseUrl": "."
   - "paths": { "@/*": ["./src/*"] }
  </action>
  <verify>
    <automated>test -f packages/ui/package.json && test -f packages/ui/tsconfig.json && grep -q "vite" packages/ui/package.json && grep -q "react" packages/ui/package.json</automated>
  </verify>
  <done>package.json and tsconfig.json created with all required dependencies and scripts</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite and Tailwind with glassmorphism theme</name>
  <files>packages/ui/vite.config.ts, packages/ui/tailwind.config.ts, packages/ui/src/styles/globals.css</files>
  <action>
Create Vite and Tailwind configuration per D-19 through D-23:

1. vite.config.ts:
   - Use @vitejs/plugin-react with SWC (fast builds)
   - Configure path alias "@/*" pointing to "./src/*"
   - Set server port to 5173 with strictPort: true

2. tailwind.config.ts with custom theme (per D-20, D-21):
   - colors.midnight: '#0B101E'
   - colors.glass.DEFAULT: 'rgba(255,255,255,0.03)'
   - colors.glass.hover: 'rgba(255,255,255,0.06)'
   - colors.glass.border: 'rgba(255,255,255,0.08)'
   - colors.azure: '#00D4FF'
   - colors.amber: '#FFB020'
   - colors.pristine: '#F8FAFC'
   - colors.clinical: '#94A3B8'
   - colors.success: '#00FF66'
   - fontFamily.sans: ['Inter', 'system-ui', 'sans-serif']
   - fontFamily.mono: ['JetBrains Mono', 'monospace']
   - boxShadow['neon-azure']: '0 0 12px rgba(0, 212, 255, 0.3)'
   - boxShadow['neon-amber']: '0 0 12px rgba(255, 176, 32, 0.3)'
   - boxShadow['inner-azure']: 'inset 0 0 12px rgba(0, 212, 255, 0.05)'

3. globals.css:
   - @import 'tailwindcss'
   - Import Google Fonts (Inter and JetBrains Mono)
   - Define @layer components:
     - .glass-panel: bg-glass, backdrop-blur-[16px], border border-glassBorder, rounded-xl, plus gradient overlay
     - .glass-panel-hover: hover:bg-glassHover
     - .status-dot-active: bg-success with shadow-[0_0_8px_rgba(0,255,102,0.6)]
   - Set body background to midnight color
  </action>
  <verify>
    <automated>test -f packages/ui/vite.config.ts && test -f packages/ui/tailwind.config.ts && test -f packages/ui/src/styles/globals.css && grep -q "midnight" packages/ui/tailwind.config.ts && grep -q "glass-panel" packages/ui/src/styles/globals.css</automated>
  </verify>
  <done>Vite and Tailwind configured with custom Architectural Cybernetics theme</done>
</task>

<task type="auto">
  <name>Task 3: Create React entry point and root App component</name>
  <files>packages/ui/index.html, packages/ui/src/main.tsx, packages/ui/src/App.tsx</files>
  <action>
Create the React application entry point per D-02 (SPA with client-side routing):

1. index.html:
   - Standard HTML5 template
   - Title: "DuckBrain — Memory Archive"
   - Link to Google Fonts (Inter and JetBrains Mono)
   - Mount point: <div id="root"></div>
   - Script: src="/src/main.tsx" type="module"

2. src/main.tsx:
   - Import React from 'react'
   - Import ReactDOM from 'react-dom/client'
   - Import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
   - Import './styles/globals.css'
   - Import App from './App'
   - Create queryClient with default options (staleTime: 30s)
   - Render with React.StrictMode

3. src/App.tsx:
   - Import { BrowserRouter, Routes, Route } from 'react-router'
   - Create basic layout component with:
     - Header with "DuckBrain" title using glass-panel styling
     - Main content area
   - Set up routes:
     - "/" → Home page placeholder
     - "/tree" → Tree view placeholder
     - "/timeline" → Timeline placeholder
   - Use glass-panel classes for visual testing

4. Create placeholder route components:
   - src/routes/Home.tsx
   - src/routes/Tree.tsx
   - src/routes/Timeline.tsx

5. Test the setup:
   - Run `npm install` in packages/ui
   - Run `npm run dev` to verify server starts
  </action>
  <verify>
    <automated>test -f packages/ui/index.html && test -f packages/ui/src/main.tsx && test -f packages/ui/src/App.tsx && grep -q "BrowserRouter" packages/ui/src/App.tsx && grep -q "QueryClientProvider" packages/ui/src/main.tsx</automated>
  </verify>
  <done>React app entry point created with QueryClient, React Router, and glassmorphism styling visible</done>
</task>

</tasks>

<verification>
- Vite dev server starts without errors
- Tailwind CSS custom colors render (azure, amber, pristine visible)
- Glassmorphism panels show backdrop blur effect
- React Router navigation works between routes
</verification>

<success_criteria>
- packages/ui/ directory exists with complete configuration
- npm install completes without errors
- npm run dev starts server on port 5173
- Glassmorphism theme visible in browser
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-01-SUMMARY.md`
</output>